# codebase-maintenance instance — bang-lang (Lean graded-CBPV kernel)

> Generated by `codebase-maintenance/BOOTSTRAP` (hand-seeded 2026-06-21 as the
> first run; regenerate when the gate command, doc set, or conventions move). A
> *derivation* of the project — the general skill holds the theory, this holds
> bang-lang's idiosyncrasies.

Binds the general procedure to this repo: the *gate*, *artifact map*, guards,
work-type routing, and surface/rung assessment below are what `BOOTSTRAP`
discovered here.

## Surface & rungs (this repo) — where it's strong, where to climb

| surface | rung here | note / climb |
|---|---|---|
| build | **test** (`just verify`) | strong; watch the false-green pipe |
| test | **test** (selfcheck differential + `sorry`/axiom burndown) | strongest surface — the burndown IS a SATD census on the test rung |
| docs | **survey** (all hand-maintained) | **climb**: link-check ADR cross-links + Reference Index (`lychee`); the burndown is the one *generated* doc |
| deps | **test-ish** (Nix `flake.lock` + Lake manifest, frozen by nix) | adequate |
| cruft | **survey** | legacy `Bang.Eval`/`Calc*` is intentional (baselined); no dead-code tool |
| debt | **test** (`sorry`/axiom burndown) + **survey** (OPEN_QUESTIONS) | exemplary — debt is *visible and counted* |
| security | **test** (`gitleaks` pre-commit, check 0, runs first + always; landed `a3a0857`) | secrets covered; SCA/dep-CVE absent but low-value (Nix-frozen Mathlib-only deps) |
| architecture | **survey** (5-primitives / STM-only / rows-are-sets are PROSE) + **test** (statement-freeze hook) | **climb**: an import-cycle / primitive-count fitness function would make Invariants 3 & 5 *structural* |
| decisions | **survey** + **test** (statement-guard) | ⚠ this repo *deleted* ADR-0003/0004 — research says **supersede, don't delete** (keeps the rejected-alternative trail). Reconsider; add a supersede-link lint |

One climb stands out now: **invariants-as-fitness-functions** (Invariants 3/5 —
five primitives, STM-only — are the project's own "make illegal states unrepresentable"
creed left on the prose rung). The **gitleaks** gap is closed (`a3a0857`).

## The gate (step 3) — real exit code

```bash
nix develop --command just verify ; echo "EXIT=$?"     # selfcheck + lake build + audit
```

NEVER `| head`/`| grep` the result and trust the exit — that is *the* false-green
in this repo (memory: `nix-build-verify-exit-codes`). The pre-commit hook already
runs `just verify` and blocks theorem-statement changes unless
`STATEMENT_CHANGE_OK=…` justifies them.

Repo-specific invariant guards (the `tools/audit.sh` rung):
- `sorry` only in theorem bodies, never in definitions / `Bang/Audit.lean`.
- No new `axiom` declarations; per-theorem `#print axioms ⊆ {propext,
  Classical.choice, Quot.sound}` (+ `sorryAx` while a body is `sorry`).
- Five kernel primitives only (thunk · force · effect rows · handlers · STM).
- Diff-scoped fast loop: `nix develop --command just check FILE` (G0/G1).

## Artifact map (step 4) — by volatility

| artifact | Diátaxis mode | volatility | when to survey |
|---|---|---|---|
| `CONTEXT.md` | volatile-state | ALWAYS | every session/checkpoint |
| `CLAUDE.md` | reference + how-to (invariants, glossary, verify-cmd) | HIGH (always-loaded) | any kernel-rule, command, or invariant change |
| `paths/PATH-*.md` | volatile-state | ALWAYS (while a path is in flight) | every in-path checkpoint |
| `ROADMAP.md` | explanation | LOW | a checkpoint (◊) moved |
| `docs/decisions/*` (ADRs) + `README` index | explanation | LOW / high-stakes | a design decision was made |
| `docs/notes/OPEN_QUESTIONS.md` | explanation | MED | a Q opened/resolved |
| `ONBOARDING.md`, `README.md` | tutorial / how-to | LOW | setup or orientation changed |
| `references/README.md` + `refs.bib` | reference | MED | a paper added or a citation changed |
| `docs/notes/spec-proof-discipline.md` | how-to | MED | PROOF_ORDER or proof conventions changed |

Note: this repo has **no generated reference** yet — every Reference doc is
hand-maintained, so each is in the survey when its surface moves. A `#print
axioms`-derived burndown (`tools/burndown.sh`) is the one *generated* artifact;
it cannot drift.

## Work-type → survey, specialized

| work-type | survey here |
|---|---|
| chore (deps, dev-env, formatting) | — (maybe `docs/notes/dev-env.md`) |
| bug fix (proof/def correction) | `CONTEXT` + the relevant `PATH`; ADR only if it overturned a decision |
| feature (new theorem/def landed) | `CONTEXT` + `PATH` + burndown; `CLAUDE.md` if an invariant/glossary term changed |
| refactor (representation/module change) | `CLAUDE.md` (architecture-in-force) + the ADRs it touches + `CONTEXT` |
| design decision | ADR (+ README index) + `OPEN_QUESTIONS` + `CONTEXT` + `ROADMAP` if ◊ moved |

## Worked example (the STD-block landing — `design decision` + `feature`)

Corrected 4 typing rules (design decision → **ADR-0021**) and proved
preservation/progress/type_safety (feature). Survey, named one by one: ADR-0021
(new) + decisions/README index + **CONTEXT** + **PATH** + **OPEN_QUESTIONS** (Q4 →
partial) + **references/README** (pinned the cbpv-ec port source to its commit) +
**spec-proof-discipline.md** — where the survey CAUGHT a falsified fact: PROOF_ORDER
#5 still said the STD block was "mechanical", the exact assumption the change
disproved. Confirm-current, no edit: **CLAUDE.md** (no invariant falsified;
CommSemiring is a typeclass refinement, not a 6th primitive), **ROADMAP** (◊2's
headline `no_accidental_handling` still unmet → ◊ did not move). Gate read the real
exit (`VERIFY_EXIT=0`, 730 jobs; axiom-clean). The drift the survey caught is the
payoff — a generic wrap leaves "mechanical" to mislead the next session.

(Prior exemplar, the de Bruijn pivot → ADR-0020/Q11/CONTEXT/PATH, lives in git
history at `d5886d9`.)
