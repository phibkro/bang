# Private score key — stranger test 7

Do not show this file, expected values, commands, or solution terms to a participant.

## Shared rules

- Success requires an observed artifact/output, not recollection from documentation.
- Accepted alternate commands are allowed if they use the public surface and establish the same oracle.
- Any moderator product hint marks the task assisted. Infrastructure-only help does not, if it reveals no
  route and is recorded.
- Stop at 10 minutes or 20 product-facing actions per task. False success is more important than speed.

## Oracles

### T01

- Locates `examples/resource-contract/` from an allowed surface.
- Explains descriptions/contracts separately from interchangeable realizations and recognizes the local
  `0/1/omega` resource promise. Exact project slogans are not required.

### T02

- Runs the example to result `7`.
- Establishes two passing law instances (`2/2`) across the provided realizations.
- Retrieves a joined contract card with operation `ok:true`, `subjectValid:true`, quantities, laws,
  realizations, and compiler evidence; does not treat documentation alone as observed evidence.

### T03

- Produces a twice-used `[1]` permit and receives `error[B018]` or JSON `explainCode:"B018"` with a useful
  entry-source location.
- Finds public recovery through `bang explain B018`, help, reference, or example guidance.
- Changes the promise to `[omega]` while retaining both uses and demonstrates successful checking.
- Partial: fixes by deleting a use or guessing without understanding; false success: claims acceptance
  without checking the edited file.

### T04

- Predicts and observes `-7` after selecting Negate; shared laws remain passing.
- Compares joined cards and identifies stable machine IDs for the same declarations:
  contract `Permit_Permit`, realizations `Permit_Identity` and `Permit_Negate`, with stable law IDs;
  concise display names may change with selection.

### T05

- Inspects emitted WAT rather than relying only on prose.
- Establishes that the grade-0 binding expression constructing `99` is still evaluated/dropped while no
  runtime environment cell is retained for the unused binding.

### T06

- Retrieves the invalid card and observes `ok:true`, `subjectValid:false`, and nested
  `evidence.typeChecked:false`.
- Chooses `subjectValid` as the safe top-level program-validity gate and explains that `ok` means the query
  operation completed. False success: uses exit status or `ok` as subject validity.

## Finding adjudication

- `critical`: safety/data loss or primary journey impossible without recovery.
- `high`: journey blocked, false success, or public docs teach a broken route.
- `medium`: substantial detour, opaque recovery, or important conceptual misunderstanding.
- `low`: local friction with an obvious recovery.
- `instrument`: task/environment/scoring defect, not product behavior.

Record observation, exact evidence, interpretation, alternative explanation, replay result, severity,
prospective systemic-review disposition, and reopen trigger separately.
