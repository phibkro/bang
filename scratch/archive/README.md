# scratch/archive — superseded probes

Exploratory probes whose question is now CLOSED by an ADR. Kept for the paper
trail (git has the full history); not built, not imported, not live.

| probe | superseded by |
|---|---|
| `AbsoluteCapsProbe.lean` | ADR-0053 absolute/level caps was **refuted** → ADR-0054 (identity representation) |
| `AbsoluteCapsStepProbe.lean` | same — the step-level absolute-caps experiment, dissolved by the ADR-0054 collapse |
| `WCKeystoneCounterProbe.lean` | the `WellCapped`-under-handler-insert counterexample, **dissolved** by the ADR-0054 identity-representation collapse |
| `MonadicRecast.lean` | ADR-0051 monadic recast was a **NO-GO** |

Live probes stay in `scratch/` (Identity*, Migration*, NonEscapeProbe — the
ADR-0054 in-flight work).
