let rec outer : Str -> Int ! {Div} = fun s =>
    let rec a : Str -> Int ! {Div} = fun t =>
        match (t : Str) {
          SNil -> 0,
          SCons(c, rest) -> $outer rest
        }
      in
    match (s : Str) {
      SNil -> 0,
      SCons(c, rest) -> $a rest
    }
  in
$outer "ab"
