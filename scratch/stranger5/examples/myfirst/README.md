# myfirst

Scaffolded by `bang new myfirst`.

- `main.bang` — the entry program (`bang run examples/myfirst/main.bang`).
- `expected.txt` — the run oracle (produced by `bang new`, re-baked by `bang test --update myfirst`); `tools/check-examples.sh` diffs `main.bang`'s stdout against it.

Replace this stub with what the example teaches.
