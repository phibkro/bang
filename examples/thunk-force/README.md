# thunk-force

The smallest description/observation example used by the contributor quickstart.
`{7}` creates a deferred computation; `$c` forces it and observes `7`.
Parentheses only group—they do not force.

```bash
lake exe bang run examples/thunk-force/main.bang    # 7
```
