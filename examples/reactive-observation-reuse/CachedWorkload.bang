-- Generated by tools/gen-reactive-workload.py; edit the generator, not this file.
-- The same 100-line DAG with one explicit observation-scoped Memo capability.

pub effect Memo {
  swap : Int -> Int
}

pub let price : Thunk (Int -> Int * Int) = {fun observed => (observed, 1)}

pub let quantity : Thunk (Int -> Int * Int) = {fun observed => (observed, 1)}

pub let unitAmount : Thunk (Cap Memo -> Int -> Int -> Int * Int ! {Memo}) =
  {fun memo => fun priceValue => fun quantityValue =>
    let hit = memo.swap(0) in
    if hit == 0
    then
      let priceMeasured = $price priceValue in
      let (p, priceCalls) = priceMeasured in
      let quantityMeasured = $quantity quantityValue in
      let (q, quantityCalls) = quantityMeasured in
      let computed = p * q in
      let stored = memo.swap(computed) in
      (computed, 1 + priceCalls + quantityCalls)
    else
      let restored = memo.swap(hit) in
      (hit, 1)}

pub let line001 : Thunk (Cap Memo -> Int -> Int -> Int * Int ! {Memo}) =
  {fun memo => fun priceValue => fun quantityValue =>
    let measured = $unitAmount memo priceValue quantityValue in
    let (amount, calls) = measured in
    (amount + 1, 1 + calls)}

pub let line002 : Thunk (Cap Memo -> Int -> Int -> Int * Int ! {Memo}) =
  {fun memo => fun priceValue => fun quantityValue =>
    let measured = $unitAmount memo priceValue quantityValue in
    let (amount, calls) = measured in
    (amount + 2, 1 + calls)}

pub let line003 : Thunk (Cap Memo -> Int -> Int -> Int * Int ! {Memo}) =
  {fun memo => fun priceValue => fun quantityValue =>
    let measured = $unitAmount memo priceValue quantityValue in
    let (amount, calls) = measured in
    (amount + 3, 1 + calls)}

pub let line004 : Thunk (Cap Memo -> Int -> Int -> Int * Int ! {Memo}) =
  {fun memo => fun priceValue => fun quantityValue =>
    let measured = $unitAmount memo priceValue quantityValue in
    let (amount, calls) = measured in
    (amount + 4, 1 + calls)}

pub let line005 : Thunk (Cap Memo -> Int -> Int -> Int * Int ! {Memo}) =
  {fun memo => fun priceValue => fun quantityValue =>
    let measured = $unitAmount memo priceValue quantityValue in
    let (amount, calls) = measured in
    (amount + 5, 1 + calls)}

pub let line006 : Thunk (Cap Memo -> Int -> Int -> Int * Int ! {Memo}) =
  {fun memo => fun priceValue => fun quantityValue =>
    let measured = $unitAmount memo priceValue quantityValue in
    let (amount, calls) = measured in
    (amount + 6, 1 + calls)}

pub let line007 : Thunk (Cap Memo -> Int -> Int -> Int * Int ! {Memo}) =
  {fun memo => fun priceValue => fun quantityValue =>
    let measured = $unitAmount memo priceValue quantityValue in
    let (amount, calls) = measured in
    (amount + 7, 1 + calls)}

pub let line008 : Thunk (Cap Memo -> Int -> Int -> Int * Int ! {Memo}) =
  {fun memo => fun priceValue => fun quantityValue =>
    let measured = $unitAmount memo priceValue quantityValue in
    let (amount, calls) = measured in
    (amount + 8, 1 + calls)}

pub let line009 : Thunk (Cap Memo -> Int -> Int -> Int * Int ! {Memo}) =
  {fun memo => fun priceValue => fun quantityValue =>
    let measured = $unitAmount memo priceValue quantityValue in
    let (amount, calls) = measured in
    (amount + 9, 1 + calls)}

pub let line010 : Thunk (Cap Memo -> Int -> Int -> Int * Int ! {Memo}) =
  {fun memo => fun priceValue => fun quantityValue =>
    let measured = $unitAmount memo priceValue quantityValue in
    let (amount, calls) = measured in
    (amount + 10, 1 + calls)}

pub let line011 : Thunk (Cap Memo -> Int -> Int -> Int * Int ! {Memo}) =
  {fun memo => fun priceValue => fun quantityValue =>
    let measured = $unitAmount memo priceValue quantityValue in
    let (amount, calls) = measured in
    (amount + 11, 1 + calls)}

pub let line012 : Thunk (Cap Memo -> Int -> Int -> Int * Int ! {Memo}) =
  {fun memo => fun priceValue => fun quantityValue =>
    let measured = $unitAmount memo priceValue quantityValue in
    let (amount, calls) = measured in
    (amount + 12, 1 + calls)}

pub let line013 : Thunk (Cap Memo -> Int -> Int -> Int * Int ! {Memo}) =
  {fun memo => fun priceValue => fun quantityValue =>
    let measured = $unitAmount memo priceValue quantityValue in
    let (amount, calls) = measured in
    (amount + 13, 1 + calls)}

pub let line014 : Thunk (Cap Memo -> Int -> Int -> Int * Int ! {Memo}) =
  {fun memo => fun priceValue => fun quantityValue =>
    let measured = $unitAmount memo priceValue quantityValue in
    let (amount, calls) = measured in
    (amount + 14, 1 + calls)}

pub let line015 : Thunk (Cap Memo -> Int -> Int -> Int * Int ! {Memo}) =
  {fun memo => fun priceValue => fun quantityValue =>
    let measured = $unitAmount memo priceValue quantityValue in
    let (amount, calls) = measured in
    (amount + 15, 1 + calls)}

pub let line016 : Thunk (Cap Memo -> Int -> Int -> Int * Int ! {Memo}) =
  {fun memo => fun priceValue => fun quantityValue =>
    let measured = $unitAmount memo priceValue quantityValue in
    let (amount, calls) = measured in
    (amount + 16, 1 + calls)}

pub let line017 : Thunk (Cap Memo -> Int -> Int -> Int * Int ! {Memo}) =
  {fun memo => fun priceValue => fun quantityValue =>
    let measured = $unitAmount memo priceValue quantityValue in
    let (amount, calls) = measured in
    (amount + 17, 1 + calls)}

pub let line018 : Thunk (Cap Memo -> Int -> Int -> Int * Int ! {Memo}) =
  {fun memo => fun priceValue => fun quantityValue =>
    let measured = $unitAmount memo priceValue quantityValue in
    let (amount, calls) = measured in
    (amount + 18, 1 + calls)}

pub let line019 : Thunk (Cap Memo -> Int -> Int -> Int * Int ! {Memo}) =
  {fun memo => fun priceValue => fun quantityValue =>
    let measured = $unitAmount memo priceValue quantityValue in
    let (amount, calls) = measured in
    (amount + 19, 1 + calls)}

pub let line020 : Thunk (Cap Memo -> Int -> Int -> Int * Int ! {Memo}) =
  {fun memo => fun priceValue => fun quantityValue =>
    let measured = $unitAmount memo priceValue quantityValue in
    let (amount, calls) = measured in
    (amount + 20, 1 + calls)}

pub let line021 : Thunk (Cap Memo -> Int -> Int -> Int * Int ! {Memo}) =
  {fun memo => fun priceValue => fun quantityValue =>
    let measured = $unitAmount memo priceValue quantityValue in
    let (amount, calls) = measured in
    (amount + 21, 1 + calls)}

pub let line022 : Thunk (Cap Memo -> Int -> Int -> Int * Int ! {Memo}) =
  {fun memo => fun priceValue => fun quantityValue =>
    let measured = $unitAmount memo priceValue quantityValue in
    let (amount, calls) = measured in
    (amount + 22, 1 + calls)}

pub let line023 : Thunk (Cap Memo -> Int -> Int -> Int * Int ! {Memo}) =
  {fun memo => fun priceValue => fun quantityValue =>
    let measured = $unitAmount memo priceValue quantityValue in
    let (amount, calls) = measured in
    (amount + 23, 1 + calls)}

pub let line024 : Thunk (Cap Memo -> Int -> Int -> Int * Int ! {Memo}) =
  {fun memo => fun priceValue => fun quantityValue =>
    let measured = $unitAmount memo priceValue quantityValue in
    let (amount, calls) = measured in
    (amount + 24, 1 + calls)}

pub let line025 : Thunk (Cap Memo -> Int -> Int -> Int * Int ! {Memo}) =
  {fun memo => fun priceValue => fun quantityValue =>
    let measured = $unitAmount memo priceValue quantityValue in
    let (amount, calls) = measured in
    (amount + 25, 1 + calls)}

pub let line026 : Thunk (Cap Memo -> Int -> Int -> Int * Int ! {Memo}) =
  {fun memo => fun priceValue => fun quantityValue =>
    let measured = $unitAmount memo priceValue quantityValue in
    let (amount, calls) = measured in
    (amount + 26, 1 + calls)}

pub let line027 : Thunk (Cap Memo -> Int -> Int -> Int * Int ! {Memo}) =
  {fun memo => fun priceValue => fun quantityValue =>
    let measured = $unitAmount memo priceValue quantityValue in
    let (amount, calls) = measured in
    (amount + 27, 1 + calls)}

pub let line028 : Thunk (Cap Memo -> Int -> Int -> Int * Int ! {Memo}) =
  {fun memo => fun priceValue => fun quantityValue =>
    let measured = $unitAmount memo priceValue quantityValue in
    let (amount, calls) = measured in
    (amount + 28, 1 + calls)}

pub let line029 : Thunk (Cap Memo -> Int -> Int -> Int * Int ! {Memo}) =
  {fun memo => fun priceValue => fun quantityValue =>
    let measured = $unitAmount memo priceValue quantityValue in
    let (amount, calls) = measured in
    (amount + 29, 1 + calls)}

pub let line030 : Thunk (Cap Memo -> Int -> Int -> Int * Int ! {Memo}) =
  {fun memo => fun priceValue => fun quantityValue =>
    let measured = $unitAmount memo priceValue quantityValue in
    let (amount, calls) = measured in
    (amount + 30, 1 + calls)}

pub let line031 : Thunk (Cap Memo -> Int -> Int -> Int * Int ! {Memo}) =
  {fun memo => fun priceValue => fun quantityValue =>
    let measured = $unitAmount memo priceValue quantityValue in
    let (amount, calls) = measured in
    (amount + 31, 1 + calls)}

pub let line032 : Thunk (Cap Memo -> Int -> Int -> Int * Int ! {Memo}) =
  {fun memo => fun priceValue => fun quantityValue =>
    let measured = $unitAmount memo priceValue quantityValue in
    let (amount, calls) = measured in
    (amount + 32, 1 + calls)}

pub let line033 : Thunk (Cap Memo -> Int -> Int -> Int * Int ! {Memo}) =
  {fun memo => fun priceValue => fun quantityValue =>
    let measured = $unitAmount memo priceValue quantityValue in
    let (amount, calls) = measured in
    (amount + 33, 1 + calls)}

pub let line034 : Thunk (Cap Memo -> Int -> Int -> Int * Int ! {Memo}) =
  {fun memo => fun priceValue => fun quantityValue =>
    let measured = $unitAmount memo priceValue quantityValue in
    let (amount, calls) = measured in
    (amount + 34, 1 + calls)}

pub let line035 : Thunk (Cap Memo -> Int -> Int -> Int * Int ! {Memo}) =
  {fun memo => fun priceValue => fun quantityValue =>
    let measured = $unitAmount memo priceValue quantityValue in
    let (amount, calls) = measured in
    (amount + 35, 1 + calls)}

pub let line036 : Thunk (Cap Memo -> Int -> Int -> Int * Int ! {Memo}) =
  {fun memo => fun priceValue => fun quantityValue =>
    let measured = $unitAmount memo priceValue quantityValue in
    let (amount, calls) = measured in
    (amount + 36, 1 + calls)}

pub let line037 : Thunk (Cap Memo -> Int -> Int -> Int * Int ! {Memo}) =
  {fun memo => fun priceValue => fun quantityValue =>
    let measured = $unitAmount memo priceValue quantityValue in
    let (amount, calls) = measured in
    (amount + 37, 1 + calls)}

pub let line038 : Thunk (Cap Memo -> Int -> Int -> Int * Int ! {Memo}) =
  {fun memo => fun priceValue => fun quantityValue =>
    let measured = $unitAmount memo priceValue quantityValue in
    let (amount, calls) = measured in
    (amount + 38, 1 + calls)}

pub let line039 : Thunk (Cap Memo -> Int -> Int -> Int * Int ! {Memo}) =
  {fun memo => fun priceValue => fun quantityValue =>
    let measured = $unitAmount memo priceValue quantityValue in
    let (amount, calls) = measured in
    (amount + 39, 1 + calls)}

pub let line040 : Thunk (Cap Memo -> Int -> Int -> Int * Int ! {Memo}) =
  {fun memo => fun priceValue => fun quantityValue =>
    let measured = $unitAmount memo priceValue quantityValue in
    let (amount, calls) = measured in
    (amount + 40, 1 + calls)}

pub let line041 : Thunk (Cap Memo -> Int -> Int -> Int * Int ! {Memo}) =
  {fun memo => fun priceValue => fun quantityValue =>
    let measured = $unitAmount memo priceValue quantityValue in
    let (amount, calls) = measured in
    (amount + 41, 1 + calls)}

pub let line042 : Thunk (Cap Memo -> Int -> Int -> Int * Int ! {Memo}) =
  {fun memo => fun priceValue => fun quantityValue =>
    let measured = $unitAmount memo priceValue quantityValue in
    let (amount, calls) = measured in
    (amount + 42, 1 + calls)}

pub let line043 : Thunk (Cap Memo -> Int -> Int -> Int * Int ! {Memo}) =
  {fun memo => fun priceValue => fun quantityValue =>
    let measured = $unitAmount memo priceValue quantityValue in
    let (amount, calls) = measured in
    (amount + 43, 1 + calls)}

pub let line044 : Thunk (Cap Memo -> Int -> Int -> Int * Int ! {Memo}) =
  {fun memo => fun priceValue => fun quantityValue =>
    let measured = $unitAmount memo priceValue quantityValue in
    let (amount, calls) = measured in
    (amount + 44, 1 + calls)}

pub let line045 : Thunk (Cap Memo -> Int -> Int -> Int * Int ! {Memo}) =
  {fun memo => fun priceValue => fun quantityValue =>
    let measured = $unitAmount memo priceValue quantityValue in
    let (amount, calls) = measured in
    (amount + 45, 1 + calls)}

pub let line046 : Thunk (Cap Memo -> Int -> Int -> Int * Int ! {Memo}) =
  {fun memo => fun priceValue => fun quantityValue =>
    let measured = $unitAmount memo priceValue quantityValue in
    let (amount, calls) = measured in
    (amount + 46, 1 + calls)}

pub let line047 : Thunk (Cap Memo -> Int -> Int -> Int * Int ! {Memo}) =
  {fun memo => fun priceValue => fun quantityValue =>
    let measured = $unitAmount memo priceValue quantityValue in
    let (amount, calls) = measured in
    (amount + 47, 1 + calls)}

pub let line048 : Thunk (Cap Memo -> Int -> Int -> Int * Int ! {Memo}) =
  {fun memo => fun priceValue => fun quantityValue =>
    let measured = $unitAmount memo priceValue quantityValue in
    let (amount, calls) = measured in
    (amount + 48, 1 + calls)}

pub let line049 : Thunk (Cap Memo -> Int -> Int -> Int * Int ! {Memo}) =
  {fun memo => fun priceValue => fun quantityValue =>
    let measured = $unitAmount memo priceValue quantityValue in
    let (amount, calls) = measured in
    (amount + 49, 1 + calls)}

pub let line050 : Thunk (Cap Memo -> Int -> Int -> Int * Int ! {Memo}) =
  {fun memo => fun priceValue => fun quantityValue =>
    let measured = $unitAmount memo priceValue quantityValue in
    let (amount, calls) = measured in
    (amount + 50, 1 + calls)}

pub let line051 : Thunk (Cap Memo -> Int -> Int -> Int * Int ! {Memo}) =
  {fun memo => fun priceValue => fun quantityValue =>
    let measured = $unitAmount memo priceValue quantityValue in
    let (amount, calls) = measured in
    (amount + 51, 1 + calls)}

pub let line052 : Thunk (Cap Memo -> Int -> Int -> Int * Int ! {Memo}) =
  {fun memo => fun priceValue => fun quantityValue =>
    let measured = $unitAmount memo priceValue quantityValue in
    let (amount, calls) = measured in
    (amount + 52, 1 + calls)}

pub let line053 : Thunk (Cap Memo -> Int -> Int -> Int * Int ! {Memo}) =
  {fun memo => fun priceValue => fun quantityValue =>
    let measured = $unitAmount memo priceValue quantityValue in
    let (amount, calls) = measured in
    (amount + 53, 1 + calls)}

pub let line054 : Thunk (Cap Memo -> Int -> Int -> Int * Int ! {Memo}) =
  {fun memo => fun priceValue => fun quantityValue =>
    let measured = $unitAmount memo priceValue quantityValue in
    let (amount, calls) = measured in
    (amount + 54, 1 + calls)}

pub let line055 : Thunk (Cap Memo -> Int -> Int -> Int * Int ! {Memo}) =
  {fun memo => fun priceValue => fun quantityValue =>
    let measured = $unitAmount memo priceValue quantityValue in
    let (amount, calls) = measured in
    (amount + 55, 1 + calls)}

pub let line056 : Thunk (Cap Memo -> Int -> Int -> Int * Int ! {Memo}) =
  {fun memo => fun priceValue => fun quantityValue =>
    let measured = $unitAmount memo priceValue quantityValue in
    let (amount, calls) = measured in
    (amount + 56, 1 + calls)}

pub let line057 : Thunk (Cap Memo -> Int -> Int -> Int * Int ! {Memo}) =
  {fun memo => fun priceValue => fun quantityValue =>
    let measured = $unitAmount memo priceValue quantityValue in
    let (amount, calls) = measured in
    (amount + 57, 1 + calls)}

pub let line058 : Thunk (Cap Memo -> Int -> Int -> Int * Int ! {Memo}) =
  {fun memo => fun priceValue => fun quantityValue =>
    let measured = $unitAmount memo priceValue quantityValue in
    let (amount, calls) = measured in
    (amount + 58, 1 + calls)}

pub let line059 : Thunk (Cap Memo -> Int -> Int -> Int * Int ! {Memo}) =
  {fun memo => fun priceValue => fun quantityValue =>
    let measured = $unitAmount memo priceValue quantityValue in
    let (amount, calls) = measured in
    (amount + 59, 1 + calls)}

pub let line060 : Thunk (Cap Memo -> Int -> Int -> Int * Int ! {Memo}) =
  {fun memo => fun priceValue => fun quantityValue =>
    let measured = $unitAmount memo priceValue quantityValue in
    let (amount, calls) = measured in
    (amount + 60, 1 + calls)}

pub let line061 : Thunk (Cap Memo -> Int -> Int -> Int * Int ! {Memo}) =
  {fun memo => fun priceValue => fun quantityValue =>
    let measured = $unitAmount memo priceValue quantityValue in
    let (amount, calls) = measured in
    (amount + 61, 1 + calls)}

pub let line062 : Thunk (Cap Memo -> Int -> Int -> Int * Int ! {Memo}) =
  {fun memo => fun priceValue => fun quantityValue =>
    let measured = $unitAmount memo priceValue quantityValue in
    let (amount, calls) = measured in
    (amount + 62, 1 + calls)}

pub let line063 : Thunk (Cap Memo -> Int -> Int -> Int * Int ! {Memo}) =
  {fun memo => fun priceValue => fun quantityValue =>
    let measured = $unitAmount memo priceValue quantityValue in
    let (amount, calls) = measured in
    (amount + 63, 1 + calls)}

pub let line064 : Thunk (Cap Memo -> Int -> Int -> Int * Int ! {Memo}) =
  {fun memo => fun priceValue => fun quantityValue =>
    let measured = $unitAmount memo priceValue quantityValue in
    let (amount, calls) = measured in
    (amount + 64, 1 + calls)}

pub let line065 : Thunk (Cap Memo -> Int -> Int -> Int * Int ! {Memo}) =
  {fun memo => fun priceValue => fun quantityValue =>
    let measured = $unitAmount memo priceValue quantityValue in
    let (amount, calls) = measured in
    (amount + 65, 1 + calls)}

pub let line066 : Thunk (Cap Memo -> Int -> Int -> Int * Int ! {Memo}) =
  {fun memo => fun priceValue => fun quantityValue =>
    let measured = $unitAmount memo priceValue quantityValue in
    let (amount, calls) = measured in
    (amount + 66, 1 + calls)}

pub let line067 : Thunk (Cap Memo -> Int -> Int -> Int * Int ! {Memo}) =
  {fun memo => fun priceValue => fun quantityValue =>
    let measured = $unitAmount memo priceValue quantityValue in
    let (amount, calls) = measured in
    (amount + 67, 1 + calls)}

pub let line068 : Thunk (Cap Memo -> Int -> Int -> Int * Int ! {Memo}) =
  {fun memo => fun priceValue => fun quantityValue =>
    let measured = $unitAmount memo priceValue quantityValue in
    let (amount, calls) = measured in
    (amount + 68, 1 + calls)}

pub let line069 : Thunk (Cap Memo -> Int -> Int -> Int * Int ! {Memo}) =
  {fun memo => fun priceValue => fun quantityValue =>
    let measured = $unitAmount memo priceValue quantityValue in
    let (amount, calls) = measured in
    (amount + 69, 1 + calls)}

pub let line070 : Thunk (Cap Memo -> Int -> Int -> Int * Int ! {Memo}) =
  {fun memo => fun priceValue => fun quantityValue =>
    let measured = $unitAmount memo priceValue quantityValue in
    let (amount, calls) = measured in
    (amount + 70, 1 + calls)}

pub let line071 : Thunk (Cap Memo -> Int -> Int -> Int * Int ! {Memo}) =
  {fun memo => fun priceValue => fun quantityValue =>
    let measured = $unitAmount memo priceValue quantityValue in
    let (amount, calls) = measured in
    (amount + 71, 1 + calls)}

pub let line072 : Thunk (Cap Memo -> Int -> Int -> Int * Int ! {Memo}) =
  {fun memo => fun priceValue => fun quantityValue =>
    let measured = $unitAmount memo priceValue quantityValue in
    let (amount, calls) = measured in
    (amount + 72, 1 + calls)}

pub let line073 : Thunk (Cap Memo -> Int -> Int -> Int * Int ! {Memo}) =
  {fun memo => fun priceValue => fun quantityValue =>
    let measured = $unitAmount memo priceValue quantityValue in
    let (amount, calls) = measured in
    (amount + 73, 1 + calls)}

pub let line074 : Thunk (Cap Memo -> Int -> Int -> Int * Int ! {Memo}) =
  {fun memo => fun priceValue => fun quantityValue =>
    let measured = $unitAmount memo priceValue quantityValue in
    let (amount, calls) = measured in
    (amount + 74, 1 + calls)}

pub let line075 : Thunk (Cap Memo -> Int -> Int -> Int * Int ! {Memo}) =
  {fun memo => fun priceValue => fun quantityValue =>
    let measured = $unitAmount memo priceValue quantityValue in
    let (amount, calls) = measured in
    (amount + 75, 1 + calls)}

pub let line076 : Thunk (Cap Memo -> Int -> Int -> Int * Int ! {Memo}) =
  {fun memo => fun priceValue => fun quantityValue =>
    let measured = $unitAmount memo priceValue quantityValue in
    let (amount, calls) = measured in
    (amount + 76, 1 + calls)}

pub let line077 : Thunk (Cap Memo -> Int -> Int -> Int * Int ! {Memo}) =
  {fun memo => fun priceValue => fun quantityValue =>
    let measured = $unitAmount memo priceValue quantityValue in
    let (amount, calls) = measured in
    (amount + 77, 1 + calls)}

pub let line078 : Thunk (Cap Memo -> Int -> Int -> Int * Int ! {Memo}) =
  {fun memo => fun priceValue => fun quantityValue =>
    let measured = $unitAmount memo priceValue quantityValue in
    let (amount, calls) = measured in
    (amount + 78, 1 + calls)}

pub let line079 : Thunk (Cap Memo -> Int -> Int -> Int * Int ! {Memo}) =
  {fun memo => fun priceValue => fun quantityValue =>
    let measured = $unitAmount memo priceValue quantityValue in
    let (amount, calls) = measured in
    (amount + 79, 1 + calls)}

pub let line080 : Thunk (Cap Memo -> Int -> Int -> Int * Int ! {Memo}) =
  {fun memo => fun priceValue => fun quantityValue =>
    let measured = $unitAmount memo priceValue quantityValue in
    let (amount, calls) = measured in
    (amount + 80, 1 + calls)}

pub let line081 : Thunk (Cap Memo -> Int -> Int -> Int * Int ! {Memo}) =
  {fun memo => fun priceValue => fun quantityValue =>
    let measured = $unitAmount memo priceValue quantityValue in
    let (amount, calls) = measured in
    (amount + 81, 1 + calls)}

pub let line082 : Thunk (Cap Memo -> Int -> Int -> Int * Int ! {Memo}) =
  {fun memo => fun priceValue => fun quantityValue =>
    let measured = $unitAmount memo priceValue quantityValue in
    let (amount, calls) = measured in
    (amount + 82, 1 + calls)}

pub let line083 : Thunk (Cap Memo -> Int -> Int -> Int * Int ! {Memo}) =
  {fun memo => fun priceValue => fun quantityValue =>
    let measured = $unitAmount memo priceValue quantityValue in
    let (amount, calls) = measured in
    (amount + 83, 1 + calls)}

pub let line084 : Thunk (Cap Memo -> Int -> Int -> Int * Int ! {Memo}) =
  {fun memo => fun priceValue => fun quantityValue =>
    let measured = $unitAmount memo priceValue quantityValue in
    let (amount, calls) = measured in
    (amount + 84, 1 + calls)}

pub let line085 : Thunk (Cap Memo -> Int -> Int -> Int * Int ! {Memo}) =
  {fun memo => fun priceValue => fun quantityValue =>
    let measured = $unitAmount memo priceValue quantityValue in
    let (amount, calls) = measured in
    (amount + 85, 1 + calls)}

pub let line086 : Thunk (Cap Memo -> Int -> Int -> Int * Int ! {Memo}) =
  {fun memo => fun priceValue => fun quantityValue =>
    let measured = $unitAmount memo priceValue quantityValue in
    let (amount, calls) = measured in
    (amount + 86, 1 + calls)}

pub let line087 : Thunk (Cap Memo -> Int -> Int -> Int * Int ! {Memo}) =
  {fun memo => fun priceValue => fun quantityValue =>
    let measured = $unitAmount memo priceValue quantityValue in
    let (amount, calls) = measured in
    (amount + 87, 1 + calls)}

pub let line088 : Thunk (Cap Memo -> Int -> Int -> Int * Int ! {Memo}) =
  {fun memo => fun priceValue => fun quantityValue =>
    let measured = $unitAmount memo priceValue quantityValue in
    let (amount, calls) = measured in
    (amount + 88, 1 + calls)}

pub let line089 : Thunk (Cap Memo -> Int -> Int -> Int * Int ! {Memo}) =
  {fun memo => fun priceValue => fun quantityValue =>
    let measured = $unitAmount memo priceValue quantityValue in
    let (amount, calls) = measured in
    (amount + 89, 1 + calls)}

pub let line090 : Thunk (Cap Memo -> Int -> Int -> Int * Int ! {Memo}) =
  {fun memo => fun priceValue => fun quantityValue =>
    let measured = $unitAmount memo priceValue quantityValue in
    let (amount, calls) = measured in
    (amount + 90, 1 + calls)}

pub let line091 : Thunk (Cap Memo -> Int -> Int -> Int * Int ! {Memo}) =
  {fun memo => fun priceValue => fun quantityValue =>
    let measured = $unitAmount memo priceValue quantityValue in
    let (amount, calls) = measured in
    (amount + 91, 1 + calls)}

pub let line092 : Thunk (Cap Memo -> Int -> Int -> Int * Int ! {Memo}) =
  {fun memo => fun priceValue => fun quantityValue =>
    let measured = $unitAmount memo priceValue quantityValue in
    let (amount, calls) = measured in
    (amount + 92, 1 + calls)}

pub let line093 : Thunk (Cap Memo -> Int -> Int -> Int * Int ! {Memo}) =
  {fun memo => fun priceValue => fun quantityValue =>
    let measured = $unitAmount memo priceValue quantityValue in
    let (amount, calls) = measured in
    (amount + 93, 1 + calls)}

pub let line094 : Thunk (Cap Memo -> Int -> Int -> Int * Int ! {Memo}) =
  {fun memo => fun priceValue => fun quantityValue =>
    let measured = $unitAmount memo priceValue quantityValue in
    let (amount, calls) = measured in
    (amount + 94, 1 + calls)}

pub let line095 : Thunk (Cap Memo -> Int -> Int -> Int * Int ! {Memo}) =
  {fun memo => fun priceValue => fun quantityValue =>
    let measured = $unitAmount memo priceValue quantityValue in
    let (amount, calls) = measured in
    (amount + 95, 1 + calls)}

pub let line096 : Thunk (Cap Memo -> Int -> Int -> Int * Int ! {Memo}) =
  {fun memo => fun priceValue => fun quantityValue =>
    let measured = $unitAmount memo priceValue quantityValue in
    let (amount, calls) = measured in
    (amount + 96, 1 + calls)}

pub let line097 : Thunk (Cap Memo -> Int -> Int -> Int * Int ! {Memo}) =
  {fun memo => fun priceValue => fun quantityValue =>
    let measured = $unitAmount memo priceValue quantityValue in
    let (amount, calls) = measured in
    (amount + 97, 1 + calls)}

pub let line098 : Thunk (Cap Memo -> Int -> Int -> Int * Int ! {Memo}) =
  {fun memo => fun priceValue => fun quantityValue =>
    let measured = $unitAmount memo priceValue quantityValue in
    let (amount, calls) = measured in
    (amount + 98, 1 + calls)}

pub let line099 : Thunk (Cap Memo -> Int -> Int -> Int * Int ! {Memo}) =
  {fun memo => fun priceValue => fun quantityValue =>
    let measured = $unitAmount memo priceValue quantityValue in
    let (amount, calls) = measured in
    (amount + 99, 1 + calls)}

pub let line100 : Thunk (Cap Memo -> Int -> Int -> Int * Int ! {Memo}) =
  {fun memo => fun priceValue => fun quantityValue =>
    let measured = $unitAmount memo priceValue quantityValue in
    let (amount, calls) = measured in
    (amount + 100, 1 + calls)}

pub let total : Thunk (Cap Memo -> Int -> Int -> Int * Int ! {Memo}) =
  {fun memo => fun priceValue => fun quantityValue =>
    let measured001 = $line001 memo priceValue quantityValue in
    let (value001, calls001) = measured001 in
    let measured002 = $line002 memo priceValue quantityValue in
    let (value002, calls002) = measured002 in
    let measured003 = $line003 memo priceValue quantityValue in
    let (value003, calls003) = measured003 in
    let measured004 = $line004 memo priceValue quantityValue in
    let (value004, calls004) = measured004 in
    let measured005 = $line005 memo priceValue quantityValue in
    let (value005, calls005) = measured005 in
    let measured006 = $line006 memo priceValue quantityValue in
    let (value006, calls006) = measured006 in
    let measured007 = $line007 memo priceValue quantityValue in
    let (value007, calls007) = measured007 in
    let measured008 = $line008 memo priceValue quantityValue in
    let (value008, calls008) = measured008 in
    let measured009 = $line009 memo priceValue quantityValue in
    let (value009, calls009) = measured009 in
    let measured010 = $line010 memo priceValue quantityValue in
    let (value010, calls010) = measured010 in
    let measured011 = $line011 memo priceValue quantityValue in
    let (value011, calls011) = measured011 in
    let measured012 = $line012 memo priceValue quantityValue in
    let (value012, calls012) = measured012 in
    let measured013 = $line013 memo priceValue quantityValue in
    let (value013, calls013) = measured013 in
    let measured014 = $line014 memo priceValue quantityValue in
    let (value014, calls014) = measured014 in
    let measured015 = $line015 memo priceValue quantityValue in
    let (value015, calls015) = measured015 in
    let measured016 = $line016 memo priceValue quantityValue in
    let (value016, calls016) = measured016 in
    let measured017 = $line017 memo priceValue quantityValue in
    let (value017, calls017) = measured017 in
    let measured018 = $line018 memo priceValue quantityValue in
    let (value018, calls018) = measured018 in
    let measured019 = $line019 memo priceValue quantityValue in
    let (value019, calls019) = measured019 in
    let measured020 = $line020 memo priceValue quantityValue in
    let (value020, calls020) = measured020 in
    let measured021 = $line021 memo priceValue quantityValue in
    let (value021, calls021) = measured021 in
    let measured022 = $line022 memo priceValue quantityValue in
    let (value022, calls022) = measured022 in
    let measured023 = $line023 memo priceValue quantityValue in
    let (value023, calls023) = measured023 in
    let measured024 = $line024 memo priceValue quantityValue in
    let (value024, calls024) = measured024 in
    let measured025 = $line025 memo priceValue quantityValue in
    let (value025, calls025) = measured025 in
    let measured026 = $line026 memo priceValue quantityValue in
    let (value026, calls026) = measured026 in
    let measured027 = $line027 memo priceValue quantityValue in
    let (value027, calls027) = measured027 in
    let measured028 = $line028 memo priceValue quantityValue in
    let (value028, calls028) = measured028 in
    let measured029 = $line029 memo priceValue quantityValue in
    let (value029, calls029) = measured029 in
    let measured030 = $line030 memo priceValue quantityValue in
    let (value030, calls030) = measured030 in
    let measured031 = $line031 memo priceValue quantityValue in
    let (value031, calls031) = measured031 in
    let measured032 = $line032 memo priceValue quantityValue in
    let (value032, calls032) = measured032 in
    let measured033 = $line033 memo priceValue quantityValue in
    let (value033, calls033) = measured033 in
    let measured034 = $line034 memo priceValue quantityValue in
    let (value034, calls034) = measured034 in
    let measured035 = $line035 memo priceValue quantityValue in
    let (value035, calls035) = measured035 in
    let measured036 = $line036 memo priceValue quantityValue in
    let (value036, calls036) = measured036 in
    let measured037 = $line037 memo priceValue quantityValue in
    let (value037, calls037) = measured037 in
    let measured038 = $line038 memo priceValue quantityValue in
    let (value038, calls038) = measured038 in
    let measured039 = $line039 memo priceValue quantityValue in
    let (value039, calls039) = measured039 in
    let measured040 = $line040 memo priceValue quantityValue in
    let (value040, calls040) = measured040 in
    let measured041 = $line041 memo priceValue quantityValue in
    let (value041, calls041) = measured041 in
    let measured042 = $line042 memo priceValue quantityValue in
    let (value042, calls042) = measured042 in
    let measured043 = $line043 memo priceValue quantityValue in
    let (value043, calls043) = measured043 in
    let measured044 = $line044 memo priceValue quantityValue in
    let (value044, calls044) = measured044 in
    let measured045 = $line045 memo priceValue quantityValue in
    let (value045, calls045) = measured045 in
    let measured046 = $line046 memo priceValue quantityValue in
    let (value046, calls046) = measured046 in
    let measured047 = $line047 memo priceValue quantityValue in
    let (value047, calls047) = measured047 in
    let measured048 = $line048 memo priceValue quantityValue in
    let (value048, calls048) = measured048 in
    let measured049 = $line049 memo priceValue quantityValue in
    let (value049, calls049) = measured049 in
    let measured050 = $line050 memo priceValue quantityValue in
    let (value050, calls050) = measured050 in
    let measured051 = $line051 memo priceValue quantityValue in
    let (value051, calls051) = measured051 in
    let measured052 = $line052 memo priceValue quantityValue in
    let (value052, calls052) = measured052 in
    let measured053 = $line053 memo priceValue quantityValue in
    let (value053, calls053) = measured053 in
    let measured054 = $line054 memo priceValue quantityValue in
    let (value054, calls054) = measured054 in
    let measured055 = $line055 memo priceValue quantityValue in
    let (value055, calls055) = measured055 in
    let measured056 = $line056 memo priceValue quantityValue in
    let (value056, calls056) = measured056 in
    let measured057 = $line057 memo priceValue quantityValue in
    let (value057, calls057) = measured057 in
    let measured058 = $line058 memo priceValue quantityValue in
    let (value058, calls058) = measured058 in
    let measured059 = $line059 memo priceValue quantityValue in
    let (value059, calls059) = measured059 in
    let measured060 = $line060 memo priceValue quantityValue in
    let (value060, calls060) = measured060 in
    let measured061 = $line061 memo priceValue quantityValue in
    let (value061, calls061) = measured061 in
    let measured062 = $line062 memo priceValue quantityValue in
    let (value062, calls062) = measured062 in
    let measured063 = $line063 memo priceValue quantityValue in
    let (value063, calls063) = measured063 in
    let measured064 = $line064 memo priceValue quantityValue in
    let (value064, calls064) = measured064 in
    let measured065 = $line065 memo priceValue quantityValue in
    let (value065, calls065) = measured065 in
    let measured066 = $line066 memo priceValue quantityValue in
    let (value066, calls066) = measured066 in
    let measured067 = $line067 memo priceValue quantityValue in
    let (value067, calls067) = measured067 in
    let measured068 = $line068 memo priceValue quantityValue in
    let (value068, calls068) = measured068 in
    let measured069 = $line069 memo priceValue quantityValue in
    let (value069, calls069) = measured069 in
    let measured070 = $line070 memo priceValue quantityValue in
    let (value070, calls070) = measured070 in
    let measured071 = $line071 memo priceValue quantityValue in
    let (value071, calls071) = measured071 in
    let measured072 = $line072 memo priceValue quantityValue in
    let (value072, calls072) = measured072 in
    let measured073 = $line073 memo priceValue quantityValue in
    let (value073, calls073) = measured073 in
    let measured074 = $line074 memo priceValue quantityValue in
    let (value074, calls074) = measured074 in
    let measured075 = $line075 memo priceValue quantityValue in
    let (value075, calls075) = measured075 in
    let measured076 = $line076 memo priceValue quantityValue in
    let (value076, calls076) = measured076 in
    let measured077 = $line077 memo priceValue quantityValue in
    let (value077, calls077) = measured077 in
    let measured078 = $line078 memo priceValue quantityValue in
    let (value078, calls078) = measured078 in
    let measured079 = $line079 memo priceValue quantityValue in
    let (value079, calls079) = measured079 in
    let measured080 = $line080 memo priceValue quantityValue in
    let (value080, calls080) = measured080 in
    let measured081 = $line081 memo priceValue quantityValue in
    let (value081, calls081) = measured081 in
    let measured082 = $line082 memo priceValue quantityValue in
    let (value082, calls082) = measured082 in
    let measured083 = $line083 memo priceValue quantityValue in
    let (value083, calls083) = measured083 in
    let measured084 = $line084 memo priceValue quantityValue in
    let (value084, calls084) = measured084 in
    let measured085 = $line085 memo priceValue quantityValue in
    let (value085, calls085) = measured085 in
    let measured086 = $line086 memo priceValue quantityValue in
    let (value086, calls086) = measured086 in
    let measured087 = $line087 memo priceValue quantityValue in
    let (value087, calls087) = measured087 in
    let measured088 = $line088 memo priceValue quantityValue in
    let (value088, calls088) = measured088 in
    let measured089 = $line089 memo priceValue quantityValue in
    let (value089, calls089) = measured089 in
    let measured090 = $line090 memo priceValue quantityValue in
    let (value090, calls090) = measured090 in
    let measured091 = $line091 memo priceValue quantityValue in
    let (value091, calls091) = measured091 in
    let measured092 = $line092 memo priceValue quantityValue in
    let (value092, calls092) = measured092 in
    let measured093 = $line093 memo priceValue quantityValue in
    let (value093, calls093) = measured093 in
    let measured094 = $line094 memo priceValue quantityValue in
    let (value094, calls094) = measured094 in
    let measured095 = $line095 memo priceValue quantityValue in
    let (value095, calls095) = measured095 in
    let measured096 = $line096 memo priceValue quantityValue in
    let (value096, calls096) = measured096 in
    let measured097 = $line097 memo priceValue quantityValue in
    let (value097, calls097) = measured097 in
    let measured098 = $line098 memo priceValue quantityValue in
    let (value098, calls098) = measured098 in
    let measured099 = $line099 memo priceValue quantityValue in
    let (value099, calls099) = measured099 in
    let measured100 = $line100 memo priceValue quantityValue in
    let (value100, calls100) = measured100 in
    (value001 + value002 + value003 + value004 + value005 + value006 + value007 + value008 + value009 + value010 + value011 + value012 + value013 + value014 + value015 + value016 + value017 + value018 + value019 + value020 + value021 + value022 + value023 + value024 + value025 + value026 + value027 + value028 + value029 + value030 + value031 + value032 + value033 + value034 + value035 + value036 + value037 + value038 + value039 + value040 + value041 + value042 + value043 + value044 + value045 + value046 + value047 + value048 + value049 + value050 + value051 + value052 + value053 + value054 + value055 + value056 + value057 + value058 + value059 + value060 + value061 + value062 + value063 + value064 + value065 + value066 + value067 + value068 + value069 + value070 + value071 + value072 + value073 + value074 + value075 + value076 + value077 + value078 + value079 + value080 + value081 + value082 + value083 + value084 + value085 + value086 + value087 + value088 + value089 + value090 + value091 + value092 + value093 + value094 + value095 + value096 + value097 + value098 + value099 + value100,
     1 + calls001 + calls002 + calls003 + calls004 + calls005 + calls006 + calls007 + calls008 + calls009 + calls010 + calls011 + calls012 + calls013 + calls014 + calls015 + calls016 + calls017 + calls018 + calls019 + calls020 + calls021 + calls022 + calls023 + calls024 + calls025 + calls026 + calls027 + calls028 + calls029 + calls030 + calls031 + calls032 + calls033 + calls034 + calls035 + calls036 + calls037 + calls038 + calls039 + calls040 + calls041 + calls042 + calls043 + calls044 + calls045 + calls046 + calls047 + calls048 + calls049 + calls050 + calls051 + calls052 + calls053 + calls054 + calls055 + calls056 + calls057 + calls058 + calls059 + calls060 + calls061 + calls062 + calls063 + calls064 + calls065 + calls066 + calls067 + calls068 + calls069 + calls070 + calls071 + calls072 + calls073 + calls074 + calls075 + calls076 + calls077 + calls078 + calls079 + calls080 + calls081 + calls082 + calls083 + calls084 + calls085 + calls086 + calls087 + calls088 + calls089 + calls090 + calls091 + calls092 + calls093 + calls094 + calls095 + calls096 + calls097 + calls098 + calls099 + calls100)}
