# Implementation Plans

Generated by the improve skill on 2026-07-10, against commit `f1cb2cf`. Execute
in the order below unless dependencies say otherwise. Each executor: read the
plan fully before starting, honor its STOP conditions, and update your row when
done.

Source: a 4-agent audit (correctness+security · tests+debt · perf+deps+dx ·
docs+direction), every finding re-verified against the code by the advisor
before planning. Zero correctness or security *bugs* were found; the plans
below are coverage, hardening, and velocity work.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Nested same-effect handler e2e oracle (identity dispatch) | P1 | S | — | DONE — MERGED to main @ 60c98ac8 |
| 002 | Clause-shape #guard matrix over handler elaboration | P1 | M | 001 (soft) | DONE — MERGED @ da8797b7; 28 cells all ACCEPT (gated negative result — no sixth family member today); curried-2-arg axis N/A (pHClause parses one binder, verified not assumed) |
| 003 | Thread `effects` through `runInferV` (fifth seeding-family member) | P2 | S | — | DONE — MERGED (verdict: LATENT) |
| 004 | Verify-loop: wire test-modules + parallelize batteries | P2 | M | 001–003 (soft) | DONE — MERGED; verify 27.1s→~9.5s warm |
| 005 | Module-path containment (symlink escape hardening) | P3 | M | — | DONE — MERGED @ e1aa7423 after one REVISE round (the plan's root-boundary premise was wrong; re-ruled entry-tree ∪ root-tree); 40/40 test-modules incl. red escape case |
| 006 | God-file seam map (design note only, zero code moves) | P3 | L | — | DONE — MERGED |
| 007 | Wire Batteries lint driver + shake/pole; first triage report | P2 | S–M | 004 | DONE — MERGED @ 8db2721c. Empirical finding: `lake lint -- A B` scopes to the LAST arg's closure only → root-module enumeration (19 roots, BFS-verified to cover all 49 files). Triage: docBlame 310 (operator-call, vs lean-comment-style.md), unusedArguments 22 (operator-call), defsWithUnderscore 17 (nolint-candidates), 13 linters clean. `lake exe pole` DOES NOT EXIST on the pinned importGraph — `pole` recipe runs `graph` (import shape) with an honest comment; real per-file timing needs a newer pin or parsing `lake build` output. See plans/007-lint-triage.md. |
| 008 | Hook env sanitization (`env -u GIT_INDEX_FILE …`) — the index-corruption vector fix | P1 | S | — | DONE — MERGED @ 222aa9ef; exercised twice under real pathspec commits, live hook carries it |
| 009 | unusedArguments fix-wave (+ install-hooks worktree fix, check-examples-env drift) | P2 | S | 007 | IN PROGRESS — running as wave-lane W6 (`fix-hygiene-009`) |
| 010 | doc-gen4 wiring: banner sweep (`/-`→`/-!`) + `docbuild/` subproject + `just docs` | P3 | M | banner sweep still held (census lanes own Meta/*) | PARTIAL @ f11837dd — the docbuild subproject + recipe LANDED but the docs build is **BLOCKED UPSTREAM**: UnicodeBasic (doc-gen4's transitive dep via BibtexQuery) gates its `extern_lib` behind isWindows → the precompiled per-module .so lacks the C symbol on Linux; NO requirer-side override exists (all four avenues traced dead against Lake 5.0.0 source). UPSTREAM VERDICT (lane upstreampr, 2026-07-10): already canonical as lean4-unicode-basic#81 — the maintainer HOLDS until Lake itself is fixed (Lake's maintainer concurs it's a Lake bug); the un-gate diff was already posted there unmerged, so no PR (our repro + nm evidence commented on #81 instead). NixOS nuance: the symbol is absent on all platforms but faults only under eager binding (bindnow — our host's default). LOCAL UNBLOCK OPTION (operator call — forking is outward): docbuild-root DIRECT `[[require]]` of a patched fork shadows the transitive dep; one fork + one lakefile line. Else wait on Lake. Banner sweep still resumes post-census. |
| 011 | Public-early plumbing: release battery + site CI (build-only) + flake-check/shake riders | P2 | M | — | DONE — MERGED @ b3973f05. `just release` (tag-local, publish commands printed never run — verified against the real remote); `site.yml` scoped to the real gap (PR validation; `pages.yml` pre-existed, the plan's premise was stale); `nix flake check` static leg (break-tested); `just shake` (single-invocation form — shake unions closures, unlike lint). Its site-build finding cascaded into the reviewer's discovery that pages.yml had NEVER completed (100 runs, 99 cancelled) — fixed @ 0b1e9cca (bang/wat fence alias at the sync seam; first-ever green local build, 411 files). |
| 012 | Tooling-stratum governance: invocation telemetry + runs-in validation + status/last-invoked columns + lane-discipline injection into agent role files | P2 | M | — | DONE — MERGED @ bf07853f. All checks demonstrated red-then-green; NATIVE $-injection into agent files EMPIRICALLY REFUTED (sentinel test w/ control) → generated-block splice landed (gen-agent-pack + fitness leg) — the derivation-ladder mechanism won anyway. |
| 013 | The ergonomics stack (9 bankable slices): S1–4 editor (GENERATED TextMate grammar from the reified rule tables → LSP stdio wrapper over Query → tree-sitter #9 → unpublished .vsix) + S5–9 agent-loop verbs (stable diagnostic codes + `bang explain` · lint FIXITS riding the rewrite preservation gate · `bang new` · `bang test --update` · watch mode) | P2/P3 | XL (a MENU across dispatches) | N3 merged (done); N5's findings note re-prioritizes S5–9 | TODO — plan written; dispatch by slice on demand |

**Trigger-gated tooling backlog** (no plan files yet; write on trigger): wasmtime/wabt into the
dev shell (trigger: W4 emission probe lands green) · `says`/tryAtEachStep proof-golf loop
(trigger: census landed + proof spine quiet; toolchain-pin risk) · build-timing script parsing
`lake build` output (trigger: plan 006's split ADR wants critical-path data) · hyperfine
micro-bench (trigger: someone actually needs a perf number; invariant #7 keeps it advisory).

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale)

## Dependency notes

- **002 after 001 (soft)**: 001's example is the run-oracle pattern 002's guards complement; 001 also smoke-tests that same-effect nesting is expressible before 002 builds matrix cells around it. Not a hard blocker.
- **004 after 001–003 (soft)**: 004 rewires the gate those plans' new tests run in; landing it last means the timing baseline and the red-path test include them. Also: 004 wires `tools/test-modules.sh` into `verify`, which 005's new test case then rides automatically.
- **006 is design-only** and independent; the *refactor it designs* is explicitly sequenced after the parked LR census unit (task #37, branch `feat-lr-carrier-stackinc-wip`) — see the plan's STOP conditions.
- 001, 002, 003 touch overlapping territory conceptually but disjoint regions (examples/ vs. TypeCheck guard region vs. TypeCheck ~line 887); if executed in parallel, use separate worktrees — this repo's rule is one writer per file.

## Follow-up findings surfaced during execution

- **A real `bang` tmLanguage grammar for the site** (deletes the sync-seam text-alias @ 0b1e9cca): the language's own site should highlight its own code — natural ◊5.75/public-early item.
- **pages.yml concurrency starvation**: 99/100 runs cancelled by push cadence — the site only deploys in quiet windows. Consider `cancel-in-progress: false` + a queue, or a path filter, so heavy repo-work days still end with a deploy.
- **`gen-tools-index.py` uses `git ls-files`** (exec011): untracked new tools silently skip `--check` validation — `git add` before `just fitness` or the check lies green. Candidate sub-check for exec012's slice-2 leg.

- **W4/emit1 landed** (`3123504c`): the ◊5.5 rung-1 probe SUCCEEDED — 4/4 pure-arithmetic programs emitted to core `.wat` and run on wasmtime match `Source.eval` (the first bang output executed outside Lean). Load-bearing hop decision: emit from the typed `Comp`, NOT `Code` (compile constant-folds closed arithmetic — the `Code` path is degenerate; rfl-witnessed in `scratch/EmitProbe.lean`). Named rung-1.5 items: **div-by-zero semantics mismatch** (kernel total `div` a/0=0 vs wasm `div_s` traps — must be resolved before div enters emitted programs), bignum vs i64, comparisons/ADT value rep. Rung-2 wall: handler-frame tracking for `try_table` scopes. Trigger FIRED: wasmtime/wabt into the dev shell (hold for exec011's flake.nix landing, then one commit).
- **W3/echo landed** (`fb8069e9`): ADR-0084 slice A — `examples/echo-mock`, a two-clause `Net { recv, send }` mock handler, 21/21 examples. Documented deviation accepted: pure-arithmetic mock instead of State-backed (Handler.custom suffices; sim-KV covers stateful handlers). Its catch — the reference's stale "#86 broken" prose — was a GENERATOR bug (`tools/gen-reference.py`), fixed at the source @ `9b61f4f8`; free prose in generated files isn't covered by the `--check` leg, which compares only the worked-example table. Reserved-word finding: `read`/`write` etc. are rejected at the PARSER, stronger than the capOpSig elaboration check.

- **`lake test` wired via `testDriver`** (operator-requested, landed `a489c797`): bare `lake test` now runs the battery driver through a leaf `Tests.lean` exe — the Lean-ecosystem-standard entry point; `just` remains the orchestration SSoT. NOTE for exec007b's merge: `lakefile.toml` gained the `testDriver` key + a `[[lean_exe]] tests` stanza — its `lintDriver` addition will conflict trivially in the same top-of-file region; resolve by keeping both.
- **`tools/install-hooks.sh` fails from linked worktrees** (exec008): `mkdir -p .git/hooks` assumes `.git` is a directory; in a linked worktree it's a gitfile. Latent bug, S fix (use `git rev-parse --git-common-dir`).

- **Pre-commit hook leaks `GIT_INDEX_FILE` into lake's git ops (exec003 — ROOT CAUSE of the recurring worktree-index corruption)**: `git commit` exports `GIT_INDEX_FILE` to hooks; the hook's `nix develop --command just verify` → lake → git-in-mathlib writes mathlib's index over the committing worktree's index. Fix: the hook (both `tools/git-hooks/pre-commit` source and installed copy) should invoke its nix/lake steps via `env -u GIT_INDEX_FILE -u GIT_DIR -u GIT_WORK_TREE …`. HIGH-value one-line-class fix; candidate plan 008.

- **`tools/check-examples-env.sh` frontmatter drift** (exec004): claims `runs-in=verify` but has no justfile recipe and is in no gate leg — pre-existing; needs either wiring or a frontmatter fix.
- **`gen-changelog.py --check` shows "≥2 commits stale" on bare `just verify` runs** (exec004): by design — the pre-commit hook regenerates and stages CHANGELOG before fitness; only looks like a defect outside the hook. No action.
- **Plan 002 note** (from exec006's evidence): the TypeCheck `#guard` corpus depends on 20+ internal names, not just the 4 public entry points — plan 002's Step-1 reading remains valid, but any corpus-extraction assumption should defer to `docs/notes/god-file-seams.md` §1.1.

## Findings considered and rejected

(So nobody re-audits them.)

- **`pub` visibility not enforced** — stale: fixed this checkpoint (issue #73); `firstPrivateUse`/`firstPrivateDotAccess` gates exist in `Bang/Frontend/TypeCheck.lean:3328-3367`. The auditor read a stranger-test note's historical finding as current.
- **Strings missing from the language reference** — stale: `docs/reference/language.md:462` has the "Strings & Characters" section (issue #65 closed).
- **docs-site npm lockfile (vocs/waku)** — rejected, with a corrected rationale (2026-07-10, operator caught the advisor's vetting error): the auditor's path was wrong (no docs-site package.json at the cited location) and the advisor first rejected it as "wrong repo" WITHOUT searching for the right path — the site actually lives at `site/` (vocs + waku + mermaid-cli, loose `^` ranges). The finding still dies, on the true grounds: `site/bun.lock` is tracked, so the dependency tree is pinned; loose ranges + a committed lockfile is the standard correct setup. Residue: if CI ever builds `site/`, use `bun install --frozen-lockfile` so the lockfile actually binds. Vetting lesson recorded: rejecting a finding for a wrong path requires searching for the right path first — mis-attributed evidence is a KNOWN auditor failure class and rejection-grounds deserve the same verification as findings.
- **paths/ hygiene (no archive markers)** — factually wrong: `paths/archive/` exists and `tools/check-paths.sh` enforces active-PATH citation in CONTEXT.md.
- **CLAUDE.md "Green means" undersells the gate** — marginal: the `just verify` command block sits directly above the criticized sentence; not worth a plan.
- **Diamond-import redundant re-parsing** — refuted: `Main.lean:233` short-circuits already-resolved modules before any file read.
- **`genBinderTable` memoization** — declined: tables are O(ctors) tiny, no real program exhibits the depth, and invariant #7 (performance is second-class) applies.
- **Lean toolchain bump (v4.30.0)** — not planned: the pin is real but the audit's lag/date figures were fabricated; treat as a periodic maintenance question for the operator, not a plan. Bumping is MED–HIGH risk (tactic drift) for no named payoff today.
- **Pre-commit "comment-only .lean diff skips verify" fast-path** — rejected inside plan 004's scope: comment-only detection in Lean diffs is fragile and the manual `BANGLANG_SKIP_VERIFY_REASON` escape already exists.
- **Seven flagged sorryAx headline theorems** — not a finding: tracked, designed, and parked in-flight work (task #37; ADR-0096); the audit confirmed the debt is transparent.
- **Error-rendering scattered across three modules** (`Diagnostic.toJson` / `runCheck` / `Query.jsonStr`) — deferred without a plan: real smell, but no concrete consumer needs a unified renderer yet; revisit when the next tool verb lands.
- **ElabFuzz clause-mutation extension** — deferred: plan 002's named matrix comes first; generative fuzzing over the same axes is its recorded follow-up (see plan 002's maintenance notes).

## Direction findings (operator options, not plans)

Recorded from the audit for the roadmap conversation; all four match the queue
already in `CONTEXT.md`:

1. **Sim-KV nondeterminism demo** — gated only on the #94 row-inference probe; highest demo-value per effort.
2. **Net/IO mock echo server** (ADR-0084 slice A) — unblocked since #44 shipped.
3. **CTR completion / carried-param binder** (#87) — stateful handler params; after sim-KV.
4. **Proof-export R1** (Q43) — wait until ~5 shipped user laws.
