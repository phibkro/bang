# bang-lang CONTEXT

> Where we are on the map RIGHT NOW. Volatile — the lead **MOVES** to the current edge;
> it never accretes a "…now also DONE" chain (that history lives in git / `CHANGELOG.md` /
> the `paths/`). **Read after `CLAUDE.md`.** Long-term map: `ROADMAP.md`.
> Research keyframes: `docs/roadmap/bang-northstar-roadmap.md`.

## Position

<!-- BEGIN GENERATED proof-state (just proof-state) — do not hand-edit -->
_Generated by `tools/gen-proof-state.py --lean-root <…>`. Proof-state for Bang tree `tree:d242d94b1fa9fc32360696072099fa94ced48d7f`; inputs `proof-input:91a7d21bfe04dda33f970123f8e12509122257515208ad49aa58b9d5981a57e9`._

- **claims:** 27 trusted-axiom (⊆ trusted-3) · 0 pending (build in flight) · 4 flagged (aliases/placeholders excluded)
- **semantic strength:** 17 strong · 4 structural · 6 bounded · 3 partial · 1 conjectural
- **enrollment roles:** 21 canonical · 10 supporting · 6 aliases · 1 placeholder
- **flagged:** `lr_sound` → [propext, sorryAx, Classical.choice, Quot.sound]
- **flagged:** `lr_fundamental` → [propext, sorryAx, Classical.choice, Quot.sound]
- **flagged:** `lr_fundamental_closed` → [propext, sorryAx, Classical.choice, Quot.sound]
- **flagged:** `zero_usage_erasable` → [propext, sorryAx]
- **placeholder:** `handler_lowering_placeholder` → [sorryAx]
- **aliases:** `Bang.Rung5ProofGrade.s5_effectful_forward_sim`, `compile_well_typed`, `handler_compiles`, `zero_grade_no_code`, `custom_program_safe`, `effect_sound`
- **sorries:** 8 (per `burndown.sh`)
<!-- END GENERATED proof-state -->

**Current edge: `PATH-compiled-browser-demo-pack` is complete and ◊5.75 is reached. Five unchanged
rung-5 modules—JSON, calculator, N-Queens, and both sim-KV realizations—execute in Node 22 and real
Chromium 143/150 with byte-exact committed outputs. The tracked static pack now carries exact producer,
source, and artifact provenance; the public page exposes only a fail-closed stdout import; repository
verification diffs every artifact against the live kernel oracle; and the production site gate runs all
five through its deployed `/bang` path in Chromium.** Production site, fitness, full verify, and
independent Fable 5 review are green. This is the last pre-◊6 checkpoint reachable by internal product
work alone; the standing operator decision still defers external exposure/organic validation, and this
landing does not spend that deferred gate.

The completed `PATH-independent-body-typing-probe` leaves the independent-typing seam
certificate-gated. Its machine-checked kill shot refutes `(Comp, claimed root type, signature)` as
sufficient. With no linker/external artifact consumer, proof-relevant certificate production remains
deferred to the same door as source provenance; `independentlyTypeValidated`, `cacheKeySafe`, and
`linkReady` remain false.

`PATH-per-binding-rhs-row-probe` found the final checker capture point but refuted both cheap attribution
strategies: duplicate source names are accepted, `#` internals are user-spellable, aliases disappear,
and monomorphization/ANF insert source-unowned outer lets. No RHS-row schema or checker behavior landed.
ADR-0117's temporary language-preservation outcome is amended by ADR-0118's independent product
evidence, but its provenance result remains binding: aggregate rows cannot be relabelled as per-binding
facts, and explicit occurrence provenance waits for a second concrete demand or gated consumer.

The census lineage remains auditable: before migration, 17 computation forms were 2 pure terminating
and 15 effectful/Div-capable, all entry-owned—14 `main`s plus `nqueens.q4/q5/q6`. After migration the
61 resolved journeys contain **233 manifest values, 24 recursive definitions, and 14 computed mains
(271 total)**. Constructor values are classified through the elaborator's real environment; eager
payloads fail closed. The former strict-initializer divergence source is now an expected B019 refusal
on check, raw-run, and slice lifecycle paths. Query dump deliberately retains structural rows for that
invalid subject while nulling checked rows and core identity. `cacheKeySafe` and `linkReady` remain false.

`PATH-module-initialization-order` remains a resolver-source occurrence inventory: duplicate-safe
`(module, sourceIndex)` rows preserve dependency-first/source order and negative provenance metadata.
ADR-0118 removes effectful/divergent non-main initialization from valid programs, so those rows no
longer imply an ordered computation contract; they still record binding construction order and must not
be promoted to elaborated identities, DCE authority, or link slots. The tracer landed at `1df84044`.

`PATH-canonical-body-artifact` now encodes the exact digest-side canonical `Comp` as
`bang-core-comp-json-v1`, decodes it strictly under byte/depth bounds, round-trips producer bytes, and
feeds decoded pure/effectful terms through the unchanged calculated VM and environment machine. The
first embedded-dump design was rejected when duplicated reachable slices broke the 100-formula journey;
`bang query body-artifact <export-id>` materializes one body on demand instead. Metadata keeps structural
round-trip separate from missing independent type validation, collision-safe addressing, and link
authority. Import slots and the consumer-side validation/link contract remain open.
The converged tracer landed at `0085f879`.

`PATH-body-artifact-integrity` adds the domain-separated
`sha256-bang-module-body-artifact-v1` address over exact artifact bytes plus sorted semantic-name /
canonical-label rows. A verified backend entry compares the trusted address before parsing, then enforces
canonical bytes and relocation/code agreement before unchanged compile/run. The e2e oracle proves that
contextual runtime-label movement preserves the address while semantic effect substitution changes it
even when code bytes coincide. This is deliberately not independent typing or linking:
`independentlyTypeValidated`, `cacheKeySafe`, and `linkReady` remain false. The converged tracer landed at
`e60d2426`.

On 2026-07-18 the operator consciously deferred **`PATH-organic-resource-validation`** and released this
product slice. That outside loop remains open: the prepared packet has not been run by an unfamiliar
developer, agentic inspection still does not substitute for a human, and no validation credit is claimed.
Resume it before ◊6/release or as soon as a participant becomes available.

The prospective systemic-review method now guards two boundaries at once: prevent protocol leakage,
overclaiming, and privacy mistakes in the external run; preserve the ownership/capability-lifetime door
without pre-building it until an allocator or actor-transfer project supplies a real protocol.

For the full paved road and intended route, read `docs/roadmap/project-roadmap.md` §“Route to the north
star.” That completed product sequence was **queryable formula DAG → measured recomputation → scoped
reuse → the concrete-Wasm capability boundary it exposed**. The immediate toolchain ordering is now
**queryable resolver DAG → measured invalidation fanout → resolved-core fingerprint → checked module
interface → stable checked interface vocabulary → scoped interface-diff consumer → declared public-law
identity → first BANG fact consumer → reachable concrete body identity → canonical digest-side effect
identity → classified slice execution boundary → syntax-bounded initializer census → attribution-refuted
RHS-row probe → manual residual audit → initialization contract → effect relocation → on-demand canonical
body artifact → collision-resistant envelope integrity → consumer-pulled independent typing/link
seam → enforcement-grade inert-initializer probe → shipped inert-top-level/entry-main contract**,
with external
correction retained as a required pre-◊6 loop. The longer line is queryable/incremental toolchain →
allocator → cooperative scheduler → filesystem → driver → verified xv6/unikernel, with the actor/Raft
branch beside it. `ROADMAP.md` remains the stable proof/checkpoint map; its parked LR seam keeps its
existing answer-in-the-frame reopening trigger.

## Where things live (SSoT — reference these, don't copy them)

| For… | Home |
|---|---|
| what's DONE · history · past milestones | `git log` · `CHANGELOG.md` |
| product definition (what/why/moat) | `docs/PRD.md` |
| the ◊ checkpoint map | `ROADMAP.md` |
| the product route from completed projects to the OS north star | `docs/roadmap/project-roadmap.md` |
| the expected-regret / prospective systemic-review method | `docs/notes/prospective-systemic-review.md` |
| decisions (why-we-chose-X) | `docs/decisions/` (generated `README.md` index) |
| per-binding effect attribution boundary | `docs/decisions/0117-per-binding-effect-facts-require-source-provenance.md` |
| shipped inert-top-level language contract | `docs/decisions/0118-top-level-declarations-are-inert-except-entry-main.md` · `paths/PATH-inert-top-level-language-contract.md` |
| completed compiled browser demo pack | `paths/PATH-compiled-browser-demo-pack.md` · `docs/demos/compiled-browser.md` · `web/docs/static/compiled-demos/manifest.json` |
| completed lattice-store core + consumer-wall probe | `paths/PATH-distributed-lattice-store.md` · `Bang/Distribution/LatticeStore.lean` · `examples/lattice-store/` |
| completed independent body-typing feasibility probe | `paths/PATH-independent-body-typing-probe.md` · `scratch/BodyTypingProbe.lean` |
| completed inert-initializer contract probe | `paths/PATH-inert-initializer-contract-probe.md` |
| body artifact integrity tracer | `paths/PATH-body-artifact-integrity.md` |
| canonical body artifact tracer | `paths/PATH-canonical-body-artifact.md` |
| completed body-effect relocation tracer | `paths/PATH-body-effect-relocations.md` |
| completed module initialization-order tracer | `paths/PATH-module-initialization-order.md` |
| completed per-binding RHS-row attribution probe | `paths/PATH-per-binding-rhs-row-probe.md` |
| completed top-level initializer census + residual audit | `paths/PATH-top-level-initializer-census.md` |
| completed slice-execution boundary tracer | `paths/PATH-slice-execution-boundary.md` |
| completed canonical body-effect identity tracer | `paths/PATH-canonical-body-effect-identity.md` |
| completed reachable module-body slice tracer | `paths/PATH-reachable-module-body-slices.md` |
| completed BANG interface-consumer tracer | `paths/PATH-bang-interface-consumer.md` |
| completed declared public-law interface tracer | `paths/PATH-module-law-contracts.md` |
| completed interface-diff consumer tracer | `paths/PATH-interface-diff-consumer.md` |
| completed stable interface-effect rendering tracer | `paths/PATH-stable-interface-effect-rendering.md` |
| completed module-interface boundary probe | `paths/PATH-module-interface-boundary-probe.md` |
| completed resolved-core fingerprint probe | `paths/PATH-core-fingerprint-probe.md` |
| completed invalidation-fanout measurement | `paths/PATH-module-invalidation-measurement.md` |
| completed queryable module-graph tracer | `paths/PATH-queryable-module-graph.md` |
| completed multi-operation first-class capability dispatch | `paths/PATH-wasm-first-class-multi-operation-caps.md` |
| active wgcexec helper-level exact capability-liveness tracer and pure/state floor boundary | `paths/PATH-wgcexec-pure-state-floor.md` · `Bang/Backend/WgcCapCode.lean` · ADR-0119 |
| completed observation-scoped reuse and stale-cache evidence | `paths/PATH-reactive-within-observation-reuse.md` |
| completed recomputation measurement and cache-decision evidence | `paths/PATH-reactive-recomputation-measurement.md` |
| completed dependency-observation evidence | `paths/PATH-reactive-dependency-observation.md` |
| completed live/stale spreadsheet tracer | `paths/PATH-reactive-spreadsheet-tracer.md` |
| deferred organic validation (required before ◊6/release) | `paths/PATH-organic-resource-validation.md` |
| completed semantic-description path records still tied from ADRs/maps | `paths/PATH-semantic-contracts.md` · `paths/PATH-stateful-handler-policy.md` · `paths/PATH-resource-contract-tracer.md` · `paths/PATH-contract-query-integrity.md` |
| deferred proof-spine (◊4 `lr_sound` seam — #15 ruled D-now/A-later) | `paths/PATH-inc5-lr-reindex.md` |
| the CLOSED-AS-PARKED lr_* census unit (α AND β both delivery-refuted at the answer-determinacy wall; resume bar = answer-in-the-frame + kernel consult) | `feat-lr-carrier-stackinc-wip @ 5fbf0d79` · ADR-0096 amendments ①–④ |
| the advisor-plan backlog (findings → plans → executor → review loop) | `plans/README.md` |
| deferred design questions | `docs/notes/OPEN_QUESTIONS.md` (generated ledger) |
| subagent roles | `.claude/agents/` |
| what's-open (issues, milestones) | GitHub Issues — github.com/phibkro/bang |

## Quick orient (fresh session)

1. `CLAUDE.md` (invariants · glossary · verify-command) → **this file** (the current edge) → `ROADMAP.md` (the map).
2. Architecture in force: ADR-0016 as revised by ADR-0059; proof-method split: ADR-0035. Proof discipline: `docs/notes/spec-proof-discipline.md`.
3. Verify: `nix develop` then `just verify` (selfcheck + build + check-examples + audit).
4. Read `paths/PATH-independent-body-typing-probe.md` for the current artifact-trust edge; the shipped
   initializer rule and its census lineage remain in `paths/PATH-inert-top-level-language-contract.md`,
   and the retained external-loop obligation remains
   `paths/PATH-organic-resource-validation.md`.

## Update discipline

The lead **MOVES** to the new edge on a checkpoint / new path / resolved blocker — it does **NOT** grow a
history chain (git · CHANGELOG · the PATH own that; growing it here is the *history-in-a-status-doc* smell —
run `/doc-smells`). Do NOT rewrite for routine commits or in-path progress — those belong in the active
`PATH-*.md`. A `@ <sha>` marks a stable conceptual waypoint (where a milestone settled), NOT live HEAD.
