# bang-lang CONTEXT

> Where we are on the map RIGHT NOW. Volatile — the lead **MOVES** to the current edge;
> it never accretes a "…now also DONE" chain (that history lives in git / `CHANGELOG.md` /
> the `paths/`). **Read after `CLAUDE.md`.** Long-term map: `ROADMAP.md`.
> Research keyframes: `docs/roadmap/bang-northstar-roadmap.md`.

## Position

<!-- BEGIN GENERATED proof-state (just proof-state) — do not hand-edit -->
_Generated by `tools/gen-proof-state.py --lean-root <…>`. Proof-state at `f826dbc`._

- **headlines:** 15 clean (⊆ trusted-3) · 0 pending (build in flight) · 8 flagged
- **flagged:** `lr_sound` → [propext, sorryAx, Classical.choice, Quot.sound]
- **flagged:** `lr_fundamental` → [propext, sorryAx, Classical.choice, Quot.sound]
- **flagged:** `lr_fundamental_closed` → [propext, sorryAx, Classical.choice, Quot.sound]
- **flagged:** `seq_unit` → [propext, sorryAx]
- **flagged:** `compile_forward_sim` → [propext, sorryAx, Classical.choice, Quot.sound]
- **flagged:** `handler_compiles` → [sorryAx]
- **flagged:** `effect_sound` → [sorryAx, Trace, traceWithin, Source.evalTrace]
- **flagged:** `zero_usage_erasable` → [propext, sorryAx]
- **sorries:** 40 (per `burndown.sh`)
<!-- END GENERATED proof-state -->

**Current edge — the polymorphism ladder is COMPLETE (`cea8ae2`, 2026-07-07):** all realized as
inference+elaboration over the monomorphic kernel (ADR-0075 elaborate-to-mono); the **verified
kernel/census UNTOUCHED the entire arc** (frontend leaves, 25 constructors, 16 headlines).

- ✅ bite-0/0b HM + higher-order `compose` · bite-1 generic data · bite-2 bounded traits (Monoid) ·
  **#55** annotation-free generic intro (parser-combinator library fully generic) · **prelude**
  Option/Result + Either-as-built-in-sum + first witnessed isomorphisms (ADR-0083) ·
  **bite-3 HKT** Functor (Case A `fmap (Some 5)⇒6` · Case B `twice` at 2 Functors⇒14) +
  **Monad** (chain⇒12 · short-circuit⇒None · laws 3/3 · Parser-as-monad do-notation "34"⇒34) — ADR-0082.
- ✅ A+B `tcon1` substrate **PRUNED** (`54f1e2b`, −75 LOC — dead code confirmed dead by removal).
- ✅ **Item 3** effect ROW-POLYMORPHISM (`5d0a32f`) — `compose : ∀ρ.…` runs at ⊥ AND {Div} (→18); parallel
  inference `IRow`, kernel `EffRow` CLOSED + untouched. **THE POLYMORPHISM ARC IS FULLY CLOSED** (bite-0→bite-3
  Monad + row-poly, kernel untouched throughout). Follow-up: **#56** single-ρ (subeffecting/full-Rémy). **#57
  DONE** (`713eb3c`) — inference row-unifier now REUSES the proven kernel `EffectRow.unify` (SSoT; mirror gone);
  a visibility-only Core touch, census byte-identical (manager-verified per-headline) — the ONLY Core touch of the
  session, and it was free.

**#44 USER-DEFINED EFFECTS arc STARTED — the moat, the first verified-spine work since the MVP (ADR-0085: general
`Handler.custom`, coexist, one-shot v1).** The IO/net prong (ADR-0084) gated on this. **Stage 1 DONE (`d84aeae`):
`Handler.custom` rep + the ADDITIVE ripple — the verified spine REOPENED and stayed axiom-clean** (census 25→26, the
+1 custom; every previously-clean headline still ⊆ trusted-three, NO new sorryAx; Spec.lean frozen statements
untouched — the ADR-0085 additive-ripple bet CONFIRMED at the kernel). custom is INERT+UNTYPED so far. **NEXT: Stage 2**
(dispatch/eval — custom actually dispatches + resumes on `Source.eval`) → **Stage 3** (typing) = a RUNNING user-defined
effect (the moat visible). Then the multi-session proof grind: **Stage 4** calc-machine (HIGH, entangled with pending
route-B ADR-0052) → 5 LR → 6 soundness → 7 surface. Staging + risk in `paths/`/ADR-0085.

Active path: **`paths/PATH-polymorphism.md`**. Design ledger: **`docs/notes/OPEN_QUESTIONS.md`** (Q1–Q42).

## Where things live (SSoT — reference these, don't copy them)

| For… | Home |
|---|---|
| what's DONE · history · past milestones | `git log` · `CHANGELOG.md` |
| product definition (what/why/moat) | `docs/PRD.md` |
| the ◊ checkpoint map | `ROADMAP.md` |
| decisions (why-we-chose-X) | `docs/decisions/` (generated `README.md` index) |
| in-flight work | `paths/PATH-*.md` |
| deferred proof-spine (◊4 `lr_sound` seam · ◊5 U5b-handler) | `paths/PATH-inc5-lr-reindex.md` · `paths/PATH-inc6-calcvm-route-b.md` |
| deferred design questions | `docs/notes/OPEN_QUESTIONS.md` (generated ledger) |
| subagent roles | `.claude/agents/` |
| what's-open (issues, milestones) | GitHub Issues — github.com/phibkro/bang |

## Quick orient (fresh session)

1. `CLAUDE.md` (invariants · glossary · verify-command) → **this file** (the current edge) → `ROADMAP.md` (the map).
2. Architecture in force: `docs/decisions/0016-two-hop-architecture-calcvm-and-wasmfx.md`. Proof discipline: `docs/notes/spec-proof-discipline.md`.
3. Verify: `nix develop` then `just verify` (selfcheck + build + check-examples + audit).
4. If a path is in flight, read its `paths/PATH-*.md`.

## Update discipline

The lead **MOVES** to the new edge on a checkpoint / new path / resolved blocker — it does **NOT** grow a
history chain (git · CHANGELOG · the PATH own that; growing it here is the *history-in-a-status-doc* smell —
run `/doc-smells`). Do NOT rewrite for routine commits or in-path progress — those belong in the active
`PATH-*.md`. A `@ <sha>` marks a stable conceptual waypoint (where a milestone settled), NOT live HEAD.
