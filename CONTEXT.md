# bang-lang CONTEXT

> Where we are on the map RIGHT NOW. Volatile — the lead **MOVES** to the current edge;
> it never accretes a "…now also DONE" chain (that history lives in git / `CHANGELOG.md` /
> the `paths/`). **Read after `CLAUDE.md`.** Long-term map: `ROADMAP.md`.
> Research keyframes: `docs/roadmap/bang-northstar-roadmap.md`.

## Position

<!-- BEGIN GENERATED proof-state (just proof-state) — do not hand-edit -->
_Generated by `tools/gen-proof-state.py --lean-root <…>`. Proof-state at `05e401d6`._

- **headlines:** 18 clean (⊆ trusted-3) · 0 pending (build in flight) · 7 flagged
- **flagged:** `lr_sound` → [propext, sorryAx, Classical.choice, Quot.sound]
- **flagged:** `lr_fundamental` → [propext, sorryAx, Classical.choice, Quot.sound]
- **flagged:** `lr_fundamental_closed` → [propext, sorryAx, Classical.choice, Quot.sound]
- **flagged:** `seq_unit` → [propext, sorryAx]
- **flagged:** `handler_compiles` → [sorryAx]
- **flagged:** `effect_sound` → [sorryAx, Trace, traceWithin, Source.evalTrace]
- **flagged:** `zero_usage_erasable` → [propext, sorryAx]
- **sorries:** 85 (per `burndown.sh`)
<!-- END GENERATED proof-state -->

**The #44 USER-DEFINED EFFECTS moat arc is COMPLETE end-to-end** — Stage 7 landed (`1284c8e`, ADR-0095): the
surface `handle e with Name as h { op(x) => body }` is implemented parser→elaborator→typer→lowering onto the
proven `Handler.custom` kernel form, e2e-verified against the kernel oracle (12/12 check-examples; tracer→30,
customResume→106, customAbortCoexist→42). Kernel untouched by construction (invariant #5); census unchanged.
Stages 2–6 (custom dispatch · `effect` decls · id-first `evalD` arm · LR custom arms · the `custom_program_safe`
soundness headline, ⊆ trusted-three) landed 2026-07-09/10 — `CHANGELOG.md` + ADR-0085/0087/0092/0095. **The edge
now MOVES to the operator-sequenced next slice** — the Stage-7 landing named compute-then-return (ADR-0065+Q27),
ahead of the carried-param binder, in queue; the IO/net prong (ADR-0084) is unblocked. Q38 module≟trait≟effect
unification survey: `docs/notes/q38-handler-surface-survey.md`.

**The flagged `lr_*` set is the last wall, and its close is now PURE EXECUTION** (feeds no clean headline):
the `crelK_fund` custom arm CLOSED (`5c2c03c`, term-measured rebuild); the cap-escape half proved VACUOUS
(`convergesC_le_escape_false`); the SKIP relocation's carrier is designed + operator-ruled — **ADR-0096
Accepted: the `StackInc` KrelS def-invariant** (shape (i′), predicate corrected from `StackBelow g` by machine
refutation; fork (B) cross-ordering premise ruled). The implementation is PARKED at
`feat-lr-carrier-stackinc-wip @ 2f3d817` (LR.lean fully green, ~28 BinaryLR sites left, all design closed —
task #37 is the board-anchored resume pin): first landing = green with `crelK_fund_up` the single residual;
second = the `crelK_fund_up` rewrite → `lr_fundamental` + `lr_fundamental_closed` shed sorryAx (census
**18→20**). `lr_sound`'s third shed needs Q22 (HELD by ruling — the single named ◊4 residual).

**Released: v0.1.0 (`bbca771`, tag `v0.1.0`)** — the ADR-0094 env/closure machine is the DEFAULT engine (the #61
per-step-`Comp.subst` fix, option A1: env semantics in the machine, substitution spec unchanged). Modules v1
(ADR-0093), the polymorphism ladder (ADR-0075/0079–0083, elaborate-to-mono over an untouched kernel), and
recursion/strings (ADR-0073/0074) all shipped into it — the verified kernel/census stayed axiom-clean throughout.

**Landed this checkpoint** (task board + `CHANGELOG.md` are the SSoT): **#62 CLOSED —
`compile_forward_sim` UNCONDITIONAL over user effects** (`d35295c`, ⊆ trusted-three; the wexec `markH` arm was
already generic — invariant #4's dividend); **#84 CLOSED — the per-stage story via the WRAPPER PATTERN**
(`47a237d`, zero kernel changes; `examples/stage-swap`; the reuse gate precisely pinned as #94); the binop
`HasCTy` rule (ADR-0065 ④, `b7269ac` — G1 pillar A; the D4 exit gate remapped by `docs/notes/ctr-design.md`:
kernel-typing wall, Q27 mis-attributed); the agent-tooling suite complete (**query · rewrite+preservation-gate ·
annotate · lint**, #80/#81/#82); the clause-binder bug family (#85/#86 + curryBind) closed; the Stage-7
reference section (#88) + `resume` reserved (#93); the effect showcase pack + stranger-test r3; four design
surveys (DBMS · OS · kernel-substrate · memory). Queued next: sim-KV completion (ctr slice 2, the ⊥-row
carve-out), #94 row-inference probe, #83 Surf-walk dedup, #87 param binder, ADR-0084 IO, #82's remaining verbs.
(ADR-0089 workspace cut stays queued.)

Next edge is operator-sequenced (no PATH open yet) — see the lead above and the task board. Design ledger: **`docs/notes/OPEN_QUESTIONS.md`** (generated; see the index).

## Where things live (SSoT — reference these, don't copy them)

| For… | Home |
|---|---|
| what's DONE · history · past milestones | `git log` · `CHANGELOG.md` |
| product definition (what/why/moat) | `docs/PRD.md` |
| the ◊ checkpoint map | `ROADMAP.md` |
| decisions (why-we-chose-X) | `docs/decisions/` (generated `README.md` index) |
| in-flight work | `paths/PATH-*.md` |
| deferred proof-spine (◊4 `lr_sound` seam — #15 ruled D-now/A-later) | `paths/PATH-inc5-lr-reindex.md` |
| the parked lr_* census unit (StackInc carrier, 18→20 — the board-anchored resume pin) | task #37 · `feat-lr-carrier-stackinc-wip @ 2f3d817` · ADR-0096 |
| deferred design questions | `docs/notes/OPEN_QUESTIONS.md` (generated ledger) |
| subagent roles | `.claude/agents/` |
| what's-open (issues, milestones) | GitHub Issues — github.com/phibkro/bang |

## Quick orient (fresh session)

1. `CLAUDE.md` (invariants · glossary · verify-command) → **this file** (the current edge) → `ROADMAP.md` (the map).
2. Architecture in force: `docs/decisions/0016-two-hop-architecture-calcvm-and-wasmfx.md`. Proof discipline: `docs/notes/spec-proof-discipline.md`.
3. Verify: `nix develop` then `just verify` (selfcheck + build + check-examples + audit).
4. If a path is in flight, read its `paths/PATH-*.md`.

## Update discipline

The lead **MOVES** to the new edge on a checkpoint / new path / resolved blocker — it does **NOT** grow a
history chain (git · CHANGELOG · the PATH own that; growing it here is the *history-in-a-status-doc* smell —
run `/doc-smells`). Do NOT rewrite for routine commits or in-path progress — those belong in the active
`PATH-*.md`. A `@ <sha>` marks a stable conceptual waypoint (where a milestone settled), NOT live HEAD.
