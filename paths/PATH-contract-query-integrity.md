# PATH-contract-query-integrity — make public evidence safe to automate against

> Repair the contract-query and diagnostic identity seams found by the first agentic inspection,
> replay the frozen study packet, then hand the slice to the organic-user loop.

## Seam

- **From checkpoint**: completed resource-contract tracer with one joined contract/quantity/
  realization/law/evidence view
- **To checkpoint**: a compatibility-conscious evidence surface whose validity, declaration identity,
  and recovery path are unambiguous across selection and output modes
- **Contract preserved**: operation success stays distinct from subject validity; public display names
  remain readable; existing quantity checking, law evidence, and route agreement do not regress

## Layer

- [ ] Kernel  [x] Compiler  [x] Surface  [x] Meta (docs/process)

## Feeds the constraint

- **Binding constraint now**: `docs/notes/stranger-test-6.md` records three independently reproduced
  evidence-seam failures: invalid subjects report top-level `ok:true`, declaration IDs churn across a
  handler-selection-only swap, and resolved human B018 output loses the stable code retained by JSON.
- **How this path feeds it**: repair the public machine contract and recovery identity before more tools
  or outsiders depend on the current meanings, then replay the exact frozen packet as a regression.

## Prospective systemic review

| concern | horizon + evidence | likelihood / impact / late cost | disposition now | reopen trigger |
|---|---|---|---|---|
| subject validity is confused with query execution | before external automation; round-6 F1, 3/3 | high / high / high once scripts consume it | **implement now**: additive explicit validity plus documented consumer rule | closed by schema + accepted/refused gates |
| declaration identity depends on selected display alias | before a second realization/tool consumer; F2, 3/3 | high / medium / high once IDs persist in tools | **implement now**: stable qualified ID separate from display name | closed by selection-swap set-equality gate |
| B018 identity and location diverge by route | before organic exposure; F3 + adjudication | high / medium / medium | **implement now**: preserve code; carry the best available source span end-to-end | full resolver source-map redesign only if the smallest span slice fails |
| example route warning flood | before organic exposure; F4 reproduced at 1,633 lines | high / medium / low | **implement now if local**: document/use the quiet built-binary route | broader warning cleanup reopens only if the quiet route cannot contain it |
| zero-parameter law witness lacks observed outcome | next failing-law consumer; F5 | medium / low / low | **preserve the door**: enrich in this batch only if the result already exists at the renderer seam | reopen on a second debug trace or a local renderer-sized patch |
| general ownership/capability lifetime system | allocator/actor consumer, neither current | low now / high eventually / high | **defer with trigger**; keep local quantities and stable identities extensible | real allocator protocol or actor transfer project is scoped |

## Acceptance matrix

| obligation | required observation |
|---|---|
| validity | accepted and refused contract cards expose unambiguous subject validity without redefining command execution success |
| stable IDs | switching only the selected realization preserves the sets of contract, realization, and law-instance IDs |
| readable names | human-facing names remain local and concise; machines never need to infer identity from them |
| diagnostic recovery | resolved human and JSON B018 both retain `B018`; the quantity annotation location is non-null when source provenance is available |
| documented route | the resource-contract example’s normal commands do not bury the result under the library warning backlog |
| regression replay | all six frozen round-6 tasks still pass and the F1–F3 expectations flip to the repaired behavior |
| standing gate | `just fitness`, `just verify`, and relevant CLI/schema/differential batteries are green |

## Plan

1. [ ] Freeze additive schema and stable-ID expectations with accepted/refused and selection-swap tests.
2. [ ] Separate query execution success from subject validity in the contract card and reference docs.
3. [ ] Add qualified declaration IDs while retaining display names as presentation.
4. [ ] Preserve B018 and the quantity source span through resolved human and JSON rendering.
5. [ ] Make the documented example route quiet; enrich the zero-parameter law witness if it remains local.
6. [ ] Replay the frozen agentic packet, run the standing gates, and prepare the repaired slice for an
   unassisted outsider run.

## Status

- [x] Started 2026-07-18
- [ ] In flight: schema and identity tests first
- [ ] Blockers: none known; source-span propagation may split into an option-preserving follow-up if it
  requires a general resolver source-map redesign
- [ ] Completed YYYY-MM-DD

## Owner

- Agent / human: operator + Codex

## Notes

This path is seam hardening, not a new product milestone. Its exit feeds the organic-user loop; it
does not count another internal inspection as human validation.
