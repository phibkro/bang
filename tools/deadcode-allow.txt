# tools/deadcode-allow.txt — declarations intentionally NOT reachable from the
# dead-code roots (Bang/Audit.lean headlines + Main's `main`), yet KEPT on purpose.
#
# Read by tools/DeadCode.lean (`just dead-code`). Each non-comment line is EITHER a
# FULLY QUALIFIED declaration name (exact park) OR a NAMESPACE PREFIX ending `.*`
# (e.g. `Bang.RunPlugReshape.*` parks that whole namespace — the SSoT form for a
# sorry-gapped-live CLUSTER: one expiry-tagged line, not a rot-prone name-by-name
# block). The analyzer subtracts these from its unreachable set so an intentional
# park does not show as noise. `just dead-code` is ADVISORY — never a hard gate — so
# this file curates signal, it does not silence a failure.
#
# Sibling of tools/refs-allow.txt (dangling-reference exceptions): same discipline —
# an entry here is a DOCUMENTED park with a reason, not a place to hide real rot.
# When you delete the parked lemma, delete its line too. A NEW unreachable decl that
# is genuinely dead should be DELETED, not allow-listed.
#
# Categories a legitimate park falls into:
#   (a) PERMANENT keep-despite-unreachable — witness/refutation lemmas imported by NO
#       headline (they exist to be *built*, as do-not-weaken regression witnesses — the
#       `Bang.+` glob compiles them; the gate is that they build, not that a headline
#       calls them); alternate-form lemmas kept beside their used twin (documented pair).
#   (b) EXPIRY-TAGGED sorry-gapped-live — a decl that IS reachable from the spine but
#       flags as dead ONLY because its consumer's proof still carries `sorry` (a sorry'd
#       arm emits no reference edge). Tag with the consumer + the expiry condition:
#         `sorry-gapped-live: <consumer> — RE-AUDIT when <headline> sheds sorryAx`.
#       These EXPIRE: when the spine closes they become truly reachable and the entry
#       must be removed. Distinct from (a), which never expires.
#
# POSTURE (task #16, 2026-07-10): while `just dead-code` reports "N/M roots carry
# sorryAx" (currently 7/26), the raw unreachable set is OVER-REPORTED — sorry'd arms
# emit no reference edges, so live-but-sorry-gapped machinery flags as false-dead
# (~510 across 28 modules on the bare run). Two mitigations, both applied:
#   - the tool STATES its own limit (the ⚠ line in `just dead-code`), so no session
#     misreads the raw count as a kill-list; and
#   - the KNOWN sorry-gapped-live CLUSTERS are parked below as category-(b) prefix
#     entries (one `.*` line each, expiry-tagged) — bounded, not a ~500-line block.
# The reliable signal that remains is a fully-CLOSED orphan island (zero live callers,
# the route-1 class). RE-RUN + re-curate the (b) entries once `lr_fundamental`/
# `lr_sound` shed sorryAx (`just axioms`) — they EXPIRE then; the true-dead set only
# becomes trustworthy at that point.
#
# ── (a) PERMANENT keep-despite-unreachable (exact name — trailing `# why`) ──

# ── (b) EXPIRY-TAGGED sorry-gapped-live (task #16, 2026-07-10) ────────────────────
# The RunPlugReshape run_plug/converges_plug_iff machinery IS reachable from the LR
# spine — `converges_plug_iff` is used at Bang/Spec.lean:241-242 (`rw […]`) — but the
# consuming proofs (lr_sound/lr_fundamental arms) still carry `sorry`, and a sorry'd
# arm emits NO constant-reference edge, so the whole cluster flags as false-dead.
# These are NOT dead; they EXPIRE from this list the moment the spine closes.
# RE-AUDIT when lr_fundamental / lr_sound shed sorryAx (`just axioms`): remove these
# two lines and re-run `just dead-code` — if the cluster is now reachable, done; if
# any member is still flagged, THAT is genuine death to investigate.
Bang.RunPlugReshape.*   # sorry-gapped-live: consumer Spec.lean:241 converges_plug_iff — RE-AUDIT when lr_fundamental sheds sorryAx
Bang.run_plug           # sorry-gapped-live: the reshape bridge (LR.lean:903) feeding converges_plug_iff — RE-AUDIT when lr_fundamental sheds sorryAx
