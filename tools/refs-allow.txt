# tools/refs-allow.txt — references that are intentionally dangling (NOT stale).
#
# Read by tools/check-refs.py. Each entry is a substring of a path reference whose
# target file no longer exists IN THE WORKING TREE but lives in the git graph — the
# single source of truth for history (CLAUDE.md doc-discipline: "history lives in git,
# not in docs"). A NEW dangling reference fails the check; these are the documented,
# greppable exceptions, each with the commit where the file lives.
#
# Cited almost entirely by RETROSPECTIVE ADRs, COMPLETED PATHs, and the CONTEXT
# "Deleted" narrative — docs that correctly record what *was*. When you delete/move a
# file and a CURRENT doc still points at it, FIX the doc; only genuinely-historical
# refs belong here.

# ── A. The retired K2 machine matrix + untyped reference ──────────────────────────
# Collapsed into Bang/CalcVM.lean at 87d5aeb (ADR-0017). Formerly kept under archive/
# as an inert corpus; archive/ was removed 2026-06-25 (git history is the preservation,
# ADR-0017 amendment). Recover any file: `git show 87d5aeb~:Bang/<file>` (pre-collapse)
# or `git show 9452660:archive/<file>` (the archived copy, before removal).
Bang/Eval.lean
Bang/Calc.lean
Bang/CalcEff.lean
Bang/CalcCBN.lean

# ── B. Pre-ADR-0016 repository layout ─────────────────────────────────────────────
# The wasmfx + oracle subtrees were flattened into the root Lean project at b537bb2
# (CONTEXT "Codebase merge"). Cited by historical ADRs/notes describing that structure.
oracle-lean/
effectrow-oracle/
bang-lang-wasmfx/
Bang/EvalJson.lean

# ── C. Exploratory spike modules ──────────────────────────────────────────────────
# Deleted after their lessons merged into the kernel (ADR-0045) / surface. Cited by the
# ADRs + completed PATHs that record the spikes. Recover via `git log --all -- <path>`.
Bang/StaticSpike.lean
Bang/SetRowSpike.lean
Bang/CapAssignSpike.lean
Bang/TightSuite2.lean

# ── D. The dead Model engine + soundness-diagonal (route-β, ADR-0063-abandoned) ────
# Bang/Model.lean (the 4323-line typeless-diagonal engine) + its 5 refute/probe
# witnesses were removed in Phase-2 restructure (#103): a closed dead island, out of
# the gated closure, proven dead by a BYTE-IDENTICAL Audit census on removal. Cited
# only by the RETROSPECTIVE ADR-0061 (which records what the diagonal WAS). Recover
# via `git show <pre-removal-sha>:Bang/Model.lean`.
Bang/Model.lean
# Bang/ReturnEscapeRefute.lean — one of the 5 diagonal refute witnesses removed in the
# same #103 commit (7adcc39); cited retrospectively by ADR-0063 + PATH-inc5. Recover via
# `git show 7adcc39~1:Bang/ReturnEscapeRefute.lean`.
Bang/ReturnEscapeRefute.lean

# ── E2. Cross-branch #16/ADR-0086 boundary witnesses ──────────────────────────────
# The two machine-checked witnesses grounding ADR-0086's premised re-freeze live on
# `origin/inc6-u5b2` (not yet merged; they land with the task-#11 wire-in unit).
# Recover: `git show origin/inc6-u5b2:scratch/<file>`.
scratch/VcapFreeRefute.lean
scratch/CustomStage1Refute.lean

# ── E. Cross-branch inc-6 U5b-handler refute-watch spike ──────────────────────────
# scratch/U5bHandlerSpike.lean (the U5b-handler refute-watch: fuel IH absorbs mint+subst,
# 5 *_composes + dispatch axiom-clean) lives on the `inc6-u5b-handler` worktree, cited by
# PATH-inc6 §U5. Not merged (a scratch de-risk spike, #65). Recover via `git show <sha>:scratch/U5bHandlerSpike.lean`.
scratch/U5bHandlerSpike.lean

# ── G. Pre-tier-restructure module names (the PATH resume-briefs' old→new map) ─────
# The flat Bang/*.lean modules were re-tiered into Core/Backend/Meta/Frontend (last seen
# at `defae6d`). PATH-inc5/inc6 (deferred-work resume briefs, 2026-07-08 slim) carry an
# explicit old→new mapping TABLE so their git-history strata stay navigable — the old
# names are the table's KEYS, intentionally historical. Recover: `git show defae6d:Bang/<file>`.
Bang/LR.lean
Bang/Compat.lean
Bang/Metatheory.lean
Bang/Operational.lean
Bang/CalcVM.lean
Bang/Compile.lean

# ── F. External toolchain source ────────────────────────────────────────────────────
# Lean TOOLCHAIN source (Std.Format home), not a repo path — cited by the #58
# formatting survey + ADR-0090 as the host Wadler engine.
Init/Data/Format/Basic.lean

# ADR-0096 (the LR freshness-carrier design consult) cites krnl3's four wall
# witnesses, which live on branch feat-lr-final-wall @ 2b0948e (the design-consult's
# evidence branch), not on this docs branch off main. They are read-only evidence
# pointers for the operator ruling; recoverable via `git show feat-lr-final-wall:<path>`.
scratch/SkipRelocateProbe.lean
scratch/AppendInvWF.lean
scratch/KrelSUnappendProbe.lean
scratch/DecompFreshStrip.lean
scratch/KrelSLengthProbe.lean
scratch/CrelKUpVacuityProbe.lean

# ── I. The generated doc-gen4 barrel (plan 010) ───────────────────────────────────
# docbuild/BangDocs.lean is REGENERATED on every `just docs` run and git-ignored
# (.gitignore:44) — a doc-only barrel that gives doc-gen4 a rooted module set (the
# parent Bang lib is a rootless glob → "0 root modules"). plans/README.md row 010
# names it in prose. Intentionally absent from the tracked tree; not stale.
docbuild/BangDocs.lean

# ── H. #52 hover design-probe spike, adopted then deleted ──────────────────────────
# docs/notes/spanned-surf-design.md §5 narrates Bang/Frontend/HoverSpike.lean, the working
# spike that PROVED decl-granularity hover composes before slice 5 (bang query hover) was
# implemented. Adopted into Bang/Frontend/Query.lean (hoverAtP/hoverJsonP/hoverJson, §4) per
# the adopt-a-spike discipline, then the spike file was deleted — the note's own "Update" in
# §1 records the adoption; these path references are RETROSPECTIVE (what the spike WAS), not
# stale pointers. Recover: `git show dd1e5e27:Bang/Frontend/HoverSpike.lean`.
Bang/Frontend/HoverSpike.lean
